- docs(PAP-92): session playbook `docs/pm/session-playbook.md` (reading order, promotion/umbrella/deferred rules, claim..end steps, stop conditions, build-loop mode section, FAQ).
- Comment templates `templates/session-{started,progress,ended}.md`, `templates/promoted.md`; footer schema `src/agents/session-footer.schema.json` and type `SessionFooter` / `PLAYBOOK_VERSION` in `src/agents/session-footer.ts`.
- `pnpm playbook:dryrun` (offline toy session, three validated comments) and `pnpm footer:validate`; `tests/playbook.test.ts` (20 footer fixtures, word count, relative links). No ADR.
- **Remediation (2026-09-19, Scribe/Sonnet 5):** footer schema → `playbookVersion: 2` (1 still validates): `character` case-insensitive plus `orchestrator`/`integrator`/`scribe`; `status` adds `review`/`reviewed`/`integrated`/`remediation`; new `verdict: "pass"|"fail"` (required on review/reviewed); `branch` required only on started/progress/ended/partial. Playbook §5.2 now cites PAP-46's mode table instead of one branch grammar; §8 rewritten for brief v1.2 (builders push `feat/*` only, integrator's merge queue lands them, In Review once contained in `main`, no direct pushes). All in-repo links are now absolute `github.com/imagine-os/empty12` URLs (repo names are temporary, noted at the top of the doc) so the byte-identical template mirror resolves them too. `session-footer.ts`, templates, `tests/playbook.test.ts` (new fixtures) and `docs/quality/review-rubrics.md` §5 (template repo) updated to match. No ADR.

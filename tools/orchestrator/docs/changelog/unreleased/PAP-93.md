- feat(PAP-93): issue contract. Pure validator `src/contract/validate.ts` (`validateIssue`, `parseSections`, `parseFilesGlobs`, exported `isOpen` with `mode: build-loop | pr-flow`), all codes incl. `BLOCKED_BY_OPEN`, `READY_BUT_BLOCKED`, `UMBRELLA_NOT_CLAIMABLE`; Triage downgraded to warnings, Canceled/Duplicate exempt.
- Webhook handler + minimal `node:http` receiver (`src/webhook/`, `pnpm contract:webhook`): bounce to Backlog, `needs-contract` label, violations comment from `templates/violations.md` with `contract-failed` footer, idempotent by `webhookId`, Justin comments-only rule, blocker-regression re-validation.
- `pnpm contract:audit [--state Backlog]` (`src/cli/contract-audit.ts`): read-only, paginated, pre-promotion list; `contract_checks` store (`src/contract/store.ts`, `node:sqlite`).
- Fixtures `fixtures/contract/` (12 good, 20 bad, 30 webhook payloads), `tests/contract/`, `docs/pm/issue-contract.md`, live audit evidence `docs/pm/audits/2026-09-19-issue-contract-audit.md`, surfaces section. Dev dep `@vitest/coverage-v8`; scripts `contract:audit`, `contract:webhook`, `test:coverage`. No ADR.

{
 "project": "agents",
 "projectName": "Agent Characters & Orgs",
 "benchmarks": [
  "Claude Code (agents, skills, hooks, plugins, MCP allowlists, worktrees, permission modes, SDK)",
  "CrewAI and AutoGen (roles, delegation)",
  "LangGraph (state graphs, handoffs)",
  "Devin and OpenHands (sandboxes, browser, secrets)",
  "LangSmith and Braintrust (tracing, datasets, prompt versioning, experiments)",
  "OpenAI Evals and Inspect (golden tasks, judges)",
  "OWASP LLM Top 10 (prompt injection, excessive agency)",
  "Vault and Teleport (short-lived credentials)"
 ],
 "featureMatrix": [
  {
   "feature": "Character schema (roles, tools, MCP, scopes, skills, memory, budget, escalation, model)",
   "coveredBy": [
    "PAP-103"
   ],
   "status": "covered",
   "note": "effort enum needs alignment with labels; amendment"
  },
  {
   "feature": "Roster YAML for nine leads and 28 subs",
   "coveredBy": [
    "PAP-284"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Lead system prompts with shared fragments and style lint",
   "coveredBy": [
    "PAP-285"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Sub-character prompts and delegation descriptions",
   "coveredBy": [
    "PAP-286"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "`.claude/agents` generation, drift check, smoke",
   "coveredBy": [
    "PAP-287"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Per-character runtime bundles and enforce-scope hook",
   "coveredBy": [
    "PAP-106",
    "r4/agents/character-bundles-and-enforce-scope-hook"
   ],
   "status": "covered",
   "note": "split this round"
  },
  {
   "feature": "Least-privilege probe suite and privilege matrix",
   "coveredBy": [
    "PAP-106",
    "r4/agents/least-privilege-probe-suite-and-matrix"
   ],
   "status": "covered",
   "note": "split this round"
  },
  {
   "feature": "Destructive-action deny list: policy and hook",
   "coveredBy": [
    "PAP-298",
    "r4/agents/deny-list-policy-file-and-hook"
   ],
   "status": "covered",
   "note": "split this round"
  },
  {
   "feature": "Approval path, MCP destructive interception, backstops",
   "coveredBy": [
    "PAP-298",
    "r4/agents/request-approval-mcp-interception-and-backstops"
   ],
   "status": "covered",
   "note": "split this round"
  },
  {
   "feature": "Prompt-injection trust tiers, wrapping, scanner, actor verification, spec freeze",
   "coveredBy": [
    "PAP-299",
    "r4/agents/trust-tiers-wrapper-scanner-and-actor-verification"
   ],
   "status": "covered",
   "note": "split this round"
  },
  {
   "feature": "Injection eval suite and canaries",
   "coveredBy": [
    "PAP-299",
    "r4/agents/injection-eval-suite-and-canaries"
   ],
   "status": "covered",
   "note": "split this round"
  },
  {
   "feature": "Runtime sandbox: container, limits, network isolation",
   "coveredBy": [
    "PAP-280"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Credential broker and egress proxy",
   "coveredBy": [
    "PAP-300"
   ],
   "status": "covered",
   "note": "pm-linear project"
  },
  {
   "feature": "Prompt and tool-call logging with redaction",
   "coveredBy": [
    "PAP-107"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Prompt-log store and browser (tracing UI)",
   "coveredBy": [
    "PAP-129",
    "PAP-135"
   ],
   "status": "covered",
   "note": "collab project"
  },
  {
   "feature": "Session observability: heartbeats, stuck detection, OTel spans",
   "coveredBy": [
    "PAP-288"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Handoff protocol and assignee switching",
   "coveredBy": [
    "PAP-108"
   ],
   "status": "covered",
   "note": "assignee switching depends on the identity decision"
  },
  {
   "feature": "Persistent memory with curation and budgets",
   "coveredBy": [
    "PAP-109"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Context packs and cache-stable prompt order",
   "coveredBy": [
    "r4/agents/docs-context-pack-and-cache-friendly-prompts"
   ],
   "status": "gap",
   "note": "150 KB read-first list per cold session"
  },
  {
   "feature": "Session-end guard hooks (footer, handoff lint, pushed branch)",
   "coveredBy": [
    "r4/agents/session-end-guard-hooks"
   ],
   "status": "gap",
   "note": "compliance relied on the model remembering"
  },
  {
   "feature": "Skills library first wave",
   "coveredBy": [
    "PAP-105"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Skills named by character sheets but unbuilt (decompose-brief, edge-case-plan, threat-model, flake-triage, plan-audit)",
   "coveredBy": [
    "r4/agents/skills-library-second-wave"
   ],
   "status": "gap",
   "note": ""
  },
  {
   "feature": "Spec authoring skill",
   "coveredBy": [
    "PAP-118"
   ],
   "status": "covered",
   "note": "spec-builder project"
  },
  {
   "feature": "Scout weekly scan routine",
   "coveredBy": [
    "PAP-218"
   ],
   "status": "covered",
   "note": "libraries project, deferred"
  },
  {
   "feature": "Slash commands for approval and handoff",
   "coveredBy": [
    "r4/agents/request-approval-mcp-interception-and-backstops",
    "PAP-108"
   ],
   "status": "partial",
   "note": "`/request-approval` in the deny-list child; `/handoff` via plugin commands"
  },
  {
   "feature": "Claude Code plugin packaging and marketplace",
   "coveredBy": [
    "r4/agents/claude-code-plugin-packaging"
   ],
   "status": "gap",
   "note": "schema lists plugins; nothing packages the org"
  },
  {
   "feature": "MCP server catalog with scope classes",
   "coveredBy": [
    "PAP-210"
   ],
   "status": "covered",
   "note": "libraries project"
  },
  {
   "feature": "Hooks library (PreToolUse, PostToolUse, Stop, SessionEnd, logging)",
   "coveredBy": [
    "PAP-106",
    "PAP-107",
    "PAP-299",
    "r4/agents/session-end-guard-hooks"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Eval harness: runner and graders",
   "coveredBy": [
    "PAP-308"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Golden task set with answer keys",
   "coveredBy": [
    "PAP-309"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "LLM judge, trend, regression issues, nightly report",
   "coveredBy": [
    "PAP-310"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Prompt versioning, change gating and experiments",
   "coveredBy": [
    "r4/agents/prompt-change-control-and-experiments"
   ],
   "status": "gap",
   "note": "LangSmith and Braintrust class; hash exists, gate does not"
  },
  {
   "feature": "Capture real sessions as eval tasks",
   "coveredBy": [
    "r4/agents/session-to-eval-task-capture"
   ],
   "status": "gap",
   "note": "deferred"
  },
  {
   "feature": "Gate 2 reviewer calibration",
   "coveredBy": [
    "PAP-241"
   ],
   "status": "covered",
   "note": "quality project"
  },
  {
   "feature": "Budgets, caps, kill switch",
   "coveredBy": [
    "PAP-111"
   ],
   "status": "covered",
   "note": "sub-agent fan-out cap; amendment"
  },
  {
   "feature": "Credit metering per character",
   "coveredBy": [
    "PAP-98"
   ],
   "status": "covered",
   "note": "pm-linear"
  },
  {
   "feature": "Vendor rate-limit governor across sessions",
   "coveredBy": [
    "r4/agents/anthropic-rate-limit-governor"
   ],
   "status": "gap",
   "note": "cost doc names it as the binding limit"
  },
  {
   "feature": "Model and effort routing per issue with fallback",
   "coveredBy": [
    "r4/pm-linear/session-model-and-effort-routing"
   ],
   "status": "covered",
   "note": "pm-linear round 4"
  },
  {
   "feature": "Second runtime or vendor adapter behind the flag",
   "coveredBy": [
    "r4/agents/second-runtime-adapter-spike"
   ],
   "status": "gap",
   "note": "deferred research"
  },
  {
   "feature": "Agents as first-class principals with keys and attribution",
   "coveredBy": [
    "PAP-60"
   ],
   "status": "covered",
   "note": "identity project"
  },
  {
   "feature": "Character identity in Linear (assignee, lead, comment author)",
   "coveredBy": [
    "r4/agents/character-linear-identity-and-attribution"
   ],
   "status": "gap",
   "note": "no bot users exist; every consumer assumes them"
  },
  {
   "feature": "Forge bot accounts and signing keys",
   "coveredBy": [
    "PAP-48"
   ],
   "status": "covered",
   "note": "forge project"
  },
  {
   "feature": "Human-in-the-loop approvals and decision cards",
   "coveredBy": [
    "PAP-94",
    "PAP-298"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Character handbook",
   "coveredBy": [
    "PAP-112"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Org chart UI with live status and controls",
   "coveredBy": [
    "PAP-113"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Agents as live participants in the product",
   "coveredBy": [
    "PAP-146"
   ],
   "status": "covered",
   "note": "realtime, soft"
  },
  {
   "feature": "Onboarding and retirement of characters with approval",
   "coveredBy": [
    "r4/agents/character-onboarding-and-retirement-wizard"
   ],
   "status": "gap",
   "note": "non-goal is autonomous hiring; nothing scaffolds approved hiring"
  },
  {
   "feature": "Sub-agent delegation with own bundles and depth caps",
   "coveredBy": [
    "PAP-106",
    "PAP-111"
   ],
   "status": "partial",
   "note": "depth and fan-out caps unspecified; amendment"
  },
  {
   "feature": "Daily standup per character",
   "coveredBy": [
    "PAP-98",
    "r4/pm-linear/always-on-operations-and-morning-report"
   ],
   "status": "partial",
   "note": "morning report covers the org; per-character view is the org chart"
  },
  {
   "feature": "Content and migration agent characters",
   "coveredBy": [
    "PAP-192",
    "PAP-208"
   ],
   "status": "covered",
   "note": "growth and migration, deferred"
  },
  {
   "feature": "Rules and skills browsable in-app",
   "coveredBy": [
    "PAP-134"
   ],
   "status": "covered",
   "note": "collab"
  },
  {
   "feature": "Agent-to-agent messaging",
   "coveredBy": [
    "PAP-108",
    "PAP-112"
   ],
   "status": "covered",
   "note": "Linear comments and mentions are the channel"
  },
  {
   "feature": "Contract package, conformance and kernel wiring",
   "coveredBy": [
    "PAP-466",
    "PAP-469",
    "PAP-472"
   ],
   "status": "covered",
   "note": "module system"
  },
  {
   "feature": "Red-team of the deny list (adversarial suite)",
   "coveredBy": [
    "r4/agents/deny-list-policy-file-and-hook"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Memory write review gate against injected imperatives",
   "coveredBy": [
    "PAP-109",
    "PAP-299"
   ],
   "status": "covered",
   "note": ""
  },
  {
   "feature": "Compaction awareness and recovery point",
   "coveredBy": [
    "PAP-92",
    "PAP-107"
   ],
   "status": "covered",
   "note": "PreCompact logged; footer is the recovery point"
  },
  {
   "feature": "Agent cost per task in evals",
   "coveredBy": [
    "PAP-308",
    "PAP-98"
   ],
   "status": "covered",
   "note": ""
  }
 ],
 "newIssues": [
  {
   "key": "r4/agents/character-bundles-and-enforce-scope-hook",
   "title": "Least privilege: per-character runtime bundles (`settings.json`, `.mcp.json`, `hooks.json`), the `enforce-scope` PreToolUse hook and `loadBundle()`",
   "type": "Build",
   "phase": "P0",
   "priority": 1,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": "PAP-106",
   "size": "M",
   "estimate": 3,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "PAP-287"
   ],
   "blocks": [
    "PAP-298",
    "r4/agents/least-privilege-probe-suite-and-matrix"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / high — Build M\n\n**Goal**\n\nFirst half of PAP-106: make the schema's `tools`, `mcpServers`, `permissionMode` and `access` real at launch time. `pnpm agents build` emits one bundle per character that the orchestrator hands to the Agent SDK, and a `PreToolUse` hook denies anything outside the bundle with a reason that names the schema field. The probe suite that proves it is the sibling child.\n\n**Scope**\n\n* In: `packages/agents/src/bundle.ts` extending the PAP-287 build with `dist/<name>/{settings.json,.mcp.json,hooks.json}`, `packages/agents/hooks/enforce-scope.ts`, `loadBundle(name)` consumed by PAP-282 `launchSession`, the always-deny set, sub-character bundle derivation (Task tool passes the sub's own bundle), stale-bundle refusal in the launcher, `docs/agents/bundles.md`.\n* Out: secrets injection (`secretsFor`, moved to PAP-300 broker placeholders), the probe suite and privilege matrix (sibling), the full deny list (PAP-298 extends this hook), container isolation (PAP-280).\n\n**Spec**\n\n* `settings.json`: `permissions.allow` from `tools.allow[]`, `permissions.deny` = always-deny set (`Bash(git push*main*)`, `Bash(rm -rf*)`, `Bash(curl*|sh)`, `Write(.claude/settings.json)`, `Write(ops/secrets/**)`) plus `tools.deny[]`, `permissions.defaultMode` from `permissionMode`; `.mcp.json` lists only `mcpServers[]` from the PAP-210 catalog with env var names, never values; `hooks.json` wires `PreToolUse` to `enforce-scope.ts` and the PAP-107 logging hooks.\n* `enforce-scope.ts` reads hook JSON from stdin, normalises Bash strings (quotes, whitespace, `$IFS`, `sh -c`) with `shell-quote`, matches path globs for file tools relative to the worktree, exact `mcp__server__tool` names with `mcp__server__*` allowed only for catalog servers marked `readOnly`; returns `permissionDecision: deny` with `reason: '<character> lacks <scope>; change tools.allow in characters/<name>.yaml'`; unknown tools fail closed; the decision is logged as `scope-denied` (PAP-107) in under 100 ms.\n* Sub-characters: `Task` invocations receive the sub's bundle derived at build time (`SUB_TOOL_NOT_IN_LEAD` already prevents widening); the hook reads `PAPEROS_CHARACTER` to pick the policy.\n* `loadBundle(name): { allowedTools, disallowedTools, permissionMode, mcpServers, hooks, envNames, bundleHash }`; the launcher refuses a bundle whose `bundleHash` differs from `roster.json.promptHash` inputs (stale build).\n* Temporary scope: label `scope:+<name>` on the issue adds one scope for that session only and is logged.\n\n**Interface contract**\n\n* Provides: bundle layout, `loadBundle()`, hook event `scope-denied`, the always-deny set constant, `scope:+<name>` label contract.\n* Consumes: PAP-287 build and `roster.json`, PAP-103 schema fields, PAP-210 catalog `readOnly` flags, PAP-107 log format, PAP-282 launcher (soft: default allowlist until wired).\n\n**Definition of done**\n\n* Bundles for 37 characters generated deterministically; `--check` in Gate 1.\n* Hook fixtures: 40 obfuscated commands (`g''it push`, `sh -c 'rm -rf /'`, path traversal, MCP wildcard on a write server) denied with the right reason; 15 benign commands allowed; p95 under 100 ms.\n* Recorded Beacon session asking to `git push origin main` denied with the schema-field reason; stale bundle refused by the launcher (test).\n* Docs; changelog; Linear comment with the fixture table and recording.\n\n**Test plan**\n\n* Unit: matcher cases, bundle snapshot per kind, hash staleness, sub-bundle derivation.\n* E2E: `claude -p` session with the bundle on a toy worktree; launcher integration with mocked SDK options.\n\n**Demo**\n\nLaunch a Beacon session in a toy worktree and ask it to `git push origin main`; read the denial naming `repo:write` and the YAML file to change. Then `pnpm agents build --check`. Ninety seconds.\n\n**Edge cases**\n\n* Tool renamed in a Claude Code release: unknown tool fails closed; the `lastVerified` alert in the nightly probe (sibling) catches it.\n* Hook script crashes: Claude Code blocks the tool on non-zero exit; `hook-error` logged.\n* Character needs a one-off scope: `scope:+<name>` with a comment; expires with the session.\n* Bundle used outside the orchestrator (`pnpm agents run`): same files, same hook; no orchestrator-only paths assumed.\n\n**Dependencies**\n\nHard: PAP-287. Soft: PAP-210, PAP-107, PAP-282, PAP-103.\n\n**Agent**\n\nBuilder: Forge (Ops Runner) with Atlas (Dispatcher) on the launcher. Reviewer: Sentinel (Security Auditor).\n\n**Size**\n\nM: one session."
  },
  {
   "key": "r4/agents/least-privilege-probe-suite-and-matrix",
   "title": "Least privilege: nightly probe suite (one allowed and one forbidden probe per tool class per character) and the generated privilege matrix",
   "type": "Build",
   "phase": "P0",
   "priority": 1,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": "PAP-106",
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "r4/agents/character-bundles-and-enforce-scope-hook"
   ],
   "blocks": [
    "PAP-298"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Build S\n\n**Goal**\n\nSecond half of PAP-106: prove the wall holds. For each of the 37 characters, one allowed and one forbidden probe per tool class (file, Bash, MCP, forge API) run nightly through real Claude Code sessions in cheap mode, and the results render the privilege matrix Sentinel reviews and the handbook (PAP-112) embeds.\n\n**Scope**\n\n* In: `packages/agents/test/least-privilege.test.ts` (probe definitions generated from `roster.json` scopes), `pnpm agents probe [--character]`, nightly workflow `agents-probes.yml` (cheap mode Sonnet 5 low, `maxTurns: 3`, cost recorded), `docs/agents/privilege-matrix.md` generated (`pnpm agents matrix --check`), `lastVerified` alerting for `KNOWN_TOOLS`, docs section.\n* Out: the bundles and hook (sibling), the full deny list and its adversarial suite (PAP-298), sandbox escape probes (PAP-280).\n\n**Spec**\n\n* Probe generator: for each character and tool class, pick one scope the character holds (allowed probe) and one it lacks (forbidden probe) from the PAP-103 scope registry; probes are one-line tasks (`read file X`, `run pnpm typecheck`, `call mcp linear issue list`, `open a PR on the forge`) with an expected decision.\n* Runner: `claude -p --agent <name>` with the bundle inside a toy worktree, `maxTurns: 3`, Sonnet 5 low; the transcript is graded by the `scope-denied` events (PAP-107) and by whether the allowed probe's tool ran; results `probes/results/<date>/<character>.json` `{ probe, expected, actual, cost }`.\n* Matrix: rows characters, columns scope classes, cells allowed, denied or unverified, generated from `roster.json` and the latest results; `--check` fails when `roster.json` changed and the matrix did not.\n* `KNOWN_TOOLS.lastVerified` older than 30 days or a probe hitting `unknown tool` opens one Linear issue for Atlas.\n* Cost cap $8 per nightly run; over cap runs a rotating subset and says so.\n\n**Interface contract**\n\n* Provides: `pnpm agents probe`, results format, `docs/agents/privilege-matrix.md`, `pnpm agents matrix --check`, nightly workflow.\n* Consumes: sibling bundles and hook, PAP-107 `scope-denied` events, PAP-287 `roster.json`, PAP-103 `SCOPES` and `KNOWN_TOOLS`, PAP-50 runner.\n\n**Definition of done**\n\n* 37-row results table attached: every forbidden probe denied, every allowed probe succeeded (or a linked fix issue).\n* Matrix generated and reviewed by Sentinel (Security Auditor) in a comment; `--check` wired into Gate 1.\n* Nightly run green for three nights under cap; `lastVerified` alert tested with a backdated fixture; docs; changelog.\n\n**Test plan**\n\n* Unit: probe generation coverage (every character, every class), grading from events, matrix rendering snapshot.\n* E2E: nightly run on the self-hosted runner with the real model in cheap mode.\n\n**Demo**\n\nRun `pnpm agents probe --character beacon` and read the eight probe rows; open `privilege-matrix.md` and find Beacon's denied `stripe:write` cell. Under one minute.\n\n**Edge cases**\n\n* Model refuses a benign allowed probe: graded `inconclusive`, retried once, never marked denied.\n* Character with no MCP servers: MCP class probes are `n/a`, shown as such.\n* Sub spawned by a lead during a probe: the sub's own denial counts for the sub's row.\n* Probe accidentally destructive (forbidden probe allowed by a bug): probes run in the sandbox (PAP-280) or a toy worktree with no credentials, so the blast radius is nil; the result is S0 for Sentinel.\n\n**Dependencies**\n\nHard: r4/agents/character-bundles-and-enforce-scope-hook. Soft: PAP-107, PAP-287, PAP-103, PAP-50, PAP-280.\n\n**Agent**\n\nBuilder: Sentinel (Security Auditor). Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/deny-list-policy-file-and-hook",
   "title": "Deny list: `ops/security/agent-deny.yaml` with 45+ rules across eleven areas, the `packages/agent-policy` matcher and explain CLI, and the PreToolUse hook extension",
   "type": "Build",
   "phase": "P0",
   "priority": 1,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": "PAP-298",
   "size": "M",
   "estimate": 3,
   "model": "Opus 5",
   "modelId": "claude-opus-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "r4/agents/character-bundles-and-enforce-scope-hook",
    "PAP-210"
   ],
   "blocks": [
    "r4/agents/request-approval-mcp-interception-and-backstops",
    "PAP-280"
   ],
   "description": "**Model / Effort:** Opus 5 (`claude-opus-5`) / high — Build M\n\n**Goal**\n\nFirst half of PAP-298: the policy and the matcher. One machine-readable file lists what no agent may do, a library evaluates a tool call against it with shell-aware parsing, the PAP-106 hook consults it after the allowlist, and `pnpm policy:explain` tells a session why. Approval path, MCP interception and backstops are the sibling child.\n\n**Scope**\n\n* In: `ops/security/agent-deny.yaml` and its Zod schema, `packages/agent-policy` (`loadPolicy()`, `evaluate()`, `compile` to `dist/agent-deny.json`, `explain`), the `enforce-scope.ts` extension that evaluates compiled rules after the allowlist, `deny-list-hit` log event, the launcher staleness check (PAP-96 refuses a stale digest), generated `docs/security/agent-deny-list.md` table, adversarial fixture corpus.\n* Out: `/request-approval` skill, MCP destructive-scope interception, backstop matrix and `pnpm policy:backstops` (sibling), sandboxing (PAP-280), token minting (PAP-300).\n\n**Spec**\n\n* Rule shape `{ id: DENY-<area>-<nn>, area, match: { tool, input: { <field>: regex } }, action: deny|approve, reason, backstop: [{ control, issue }], severity: S0|S1 }`; areas `git`, `fs`, `db`, `linear`, `forge`, `stripe`, `payroll`, `infra`, `secrets`, `comms`, `agents`; the initial 45 rules are the PAP-298 Spec list verbatim, covering all eleven areas.\n* Matcher: tokenises Bash with `shell-quote` and evaluates every sub-command across `&&`, `;`, `|`, `sh -c`, heredocs, `xargs` and `eval`; unparseable commands deny with `DENY-SHELL-00`; path rules resolve relative to the worktree; SQL bodies (Drizzle raw, `psql -c`) and Linear GraphQL documents are parsed for the db and linear rules.\n* Hook order: allowlist (PAP-106) first, then policy; `deny` returns `permissionDecision: deny` with `DENY-<id>: <reason>. Use /request-approval if this is required.`; `approve` adds the approval hint; every hit logs `deny-list-hit { rule, character, issue, commandHash }` through PAP-107; three S0 hits in one session emit `agent.policy.strike3` for PAP-111.\n* `pnpm policy:build` compiles YAML to JSON with a digest; the PAP-282 launcher and the PAP-280 image compare digests and refuse stale policy; `pnpm policy:explain \"<command>\"` prints matched rules.\n* Profiles: the `justin` operator profile denies only the secrets area; Atlas keeps `approve` on destructive Linear and forge operations.\n\n**Interface contract**\n\n* Provides: `agent-deny.yaml` schema and file, `loadPolicy()`, `evaluate(toolName, toolInput, ctx)`, compiled JSON with digest, events `deny-list-hit` and `agent.policy.strike3`, `pnpm policy:build|explain`, generated docs table.\n* Consumes: the PAP-106 hook and bundles (sibling issue in this round), PAP-210 scope classes, PAP-107 logging, PAP-282 launcher, PAP-280 image, PAP-219 `controls.yaml` ids `SEC-AGENT-*`.\n\n**Definition of done**\n\n* 45+ rules across eleven areas validate; every S0 rule names at least one backstop (the sibling fills the matrix; here the field is required).\n* Matcher fixtures: 200 commands, 40 paraphrases (`git push -f`, `--force-with-lease`, `rm -r -f`, `psql -c \"delete from users\"`) denied and 40 benign near-misses (`git push origin feat/PAP-42`, `rm -rf node_modules` in the worktree) allowed; zero false positives on the benign set.\n* Recorded Forge session denied `git push --force origin main` with `DENY-GIT-01`; stale digest refused (test).\n* Docs table generated; changelog under \"Security\"; Linear comment with the fixture results.\n\n**Test plan**\n\n* Unit: tokeniser and sub-command walk, regex per rule, path containment, SQL and GraphQL parsing, digest staleness, strike counter.\n* E2E: `claude -p` sessions at low effort with `maxTurns: 3` against the hook using the PAP-106 harness.\n\n**Demo**\n\nAsk a Forge session to `cd /tmp && git push --force origin main`; the hook denies with `DENY-GIT-01` and the reason. Run `pnpm policy:explain 'psql -c \"truncate users\"'` and read the matched db rule. Under one minute.\n\n**Edge cases**\n\n* Legitimate migration file containing `DROP TABLE`: writing the file is allowed; executing it is `approve` unless the target database matches `paperos_dev_*` (PAP-42).\n* Rule too broad blocks an issue: session posts the rule id, returns the issue to Backlog with `policy-blocked`; Sentinel reviews weekly.\n* Command with secrets in arguments: only a hash is logged.\n* Policy file edited by a non-Sentinel character: itself a `DENY-AGENTS-*` hit and a CODEOWNERS block (PAP-305).\n\n**Dependencies**\n\nHard: r4/agents/character-bundles-and-enforce-scope-hook, PAP-210. Soft: PAP-107, PAP-282, PAP-280, PAP-219, PAP-305, PAP-42.\n\n**Agent**\n\nBuilder: Sentinel (Security Auditor). Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nM: one session."
  },
  {
   "key": "r4/agents/request-approval-mcp-interception-and-backstops",
   "title": "Deny list: the `/request-approval` skill filing a Needs Justin card, MCP destructive-scope interception per character and the backstop matrix with `pnpm policy:backstops`",
   "type": "Build",
   "phase": "P0",
   "priority": 1,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": "PAP-298",
   "size": "S",
   "estimate": 2,
   "model": "Opus 5",
   "modelId": "claude-opus-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "r4/agents/deny-list-policy-file-and-hook",
    "PAP-94"
   ],
   "blocks": [
    "PAP-111",
    "PAP-96"
   ],
   "description": "**Model / Effort:** Opus 5 (`claude-opus-5`) / high — Build S\n\n**Goal**\n\nSecond half of PAP-298: what happens when a blocked action is genuinely needed, how destructive MCP tools disappear from every character except Atlas, and the proof that every S0 rule is also enforced server-side so a bypassed hook still fails. Without the backstop matrix the deny list is advice; with it, it is a control Sentinel can audit.\n\n**Scope**\n\n* In: `.claude/skills/request-approval/` (SKILL.md, `scripts/file-card.ts`) producing a PAP-94 decision card and ending the session with footer `status: \"approval-requested\"`; orchestrator handling of the approved re-queue (`APPROVED_ACTION` in the prompt); bundle generator extension (PAP-106 sibling) removing `scope: destructive` MCP tools for all but Atlas and routing Atlas's through `approve`; `ops/security/backstops.yaml` mapping each rule to its independent control and owning issue; `pnpm policy:backstops` CI check; docs sections.\n* Out: the rules and matcher (sibling), the controls themselves (forge rulesets PAP-46, Postgres roles PAP-30 and PAP-34, restricted Stripe keys PAP-359, Linear write proxy PAP-300).\n\n**Spec**\n\n* `/request-approval <action>`: validates the action against the policy (it must match an `approve` or `deny` rule), files a card `Decision needed: run <action>` with recommendation, blast radius from the rule's `reason`, rollback plan, options `approve | reject`, deadline 48 h, `hard-block: true` for S0; moves nothing; ends the session with the footer; the orchestrator re-queues the issue when Justin replies `approve`, injecting `APPROVED_ACTION: <id> <action> approved <at>` into the prompt so the hook allows exactly that command once (matched by hash) and logs `approved-action-used`.\n* MCP interception: the PAP-210 catalog marks tools `scope: destructive`; the bundle generator wraps those servers with a tool filter so destructive tools are absent from every character's tool list except Atlas, whose calls hit `approve`; a probe in the PAP-106 probe suite asserts the tool list per character.\n* Backstops: `backstops.yaml` `{ ruleId, control, issue, verify: test|manual }`; initial matrix: git rules to forge rulesets (PAP-46), db rules to `paperos_app` `NOBYPASSRLS` and tailnet-only Postgres (PAP-30, PAP-34), Linear rules to the write proxy allowlist (PAP-300), forge rules to bot token scopes (PAP-48), Stripe to restricted test keys (PAP-359), infra and secrets to credentials absent from sessions (PAP-300, PAP-280), comms to sandbox mode (PAP-190, PAP-191), agents to CODEOWNERS (PAP-305).\n* `pnpm policy:backstops` fails CI when any S0 rule lacks a backstop or names an issue that is Canceled; PAP-219 `controls.yaml` gains `SEC-AGENT-*` entries generated from the matrix.\n\n**Interface contract**\n\n* Provides: skill `/request-approval`, footer status `approval-requested`, prompt variable `APPROVED_ACTION`, event `approved-action-used`, `backstops.yaml` schema, `pnpm policy:backstops`, MCP tool filter in bundles.\n* Consumes: sibling policy and hook, PAP-94 `requestDecision()` and grammar, PAP-96 re-queue, PAP-106 bundles, PAP-210 scope classes, PAP-219 controls.\n\n**Definition of done**\n\n* `/request-approval` on a rehearsal issue produces a valid card; Justin's `approve` re-queues the issue and the approved command runs once and is logged (recording).\n* MCP destructive tools absent from a Forge session's tool list, present for Atlas but denied and escalated (recording).\n* `pnpm policy:backstops` passes with every S0 rule mapped; a seeded rule without a backstop fails CI (test).\n* Docs; changelog under \"Security\"; Linear comment with recordings.\n\n**Test plan**\n\n* Unit: card rendering from a rule, approved-action hash match and single use, tool filter per character, backstop coverage check.\n* E2E: rehearsal approval round trip on staging.\n\n**Demo**\n\nIn a Forge session run `/request-approval 'docker volume rm paperos_staging'`; watch the card appear in Needs Justin; reply `approve`; watch the re-queued session run exactly that command and the hook log `approved-action-used`. Two minutes.\n\n**Edge cases**\n\n* Approved action drifts (different flags): hash mismatch denies; the session must request again with the exact command.\n* Justin approves after the deadline: card expired; a new request is needed (no stale approvals).\n* Backstop control is itself down (Forgejo offline): the hook still denies; the matrix documents which layer held.\n* Atlas needs a destructive Linear action during planning: the write proxy (PAP-300) is the backstop and refuses regardless of the hook.\n\n**Dependencies**\n\nHard: r4/agents/deny-list-policy-file-and-hook, PAP-94. Soft: PAP-96, PAP-106, PAP-210, PAP-219, PAP-300, PAP-46, PAP-48, PAP-359, PAP-305.\n\n**Agent**\n\nBuilder: Sentinel (Security Auditor) with Atlas (Dispatcher) for the re-queue. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/trust-tiers-wrapper-scanner-and-actor-verification",
   "title": "Prompt injection: trust-tier classifier, `<untrusted>` wrapping in the prompt renderer, injection scanner, T1 actor verification for the reply grammar and the spec-freeze hash",
   "type": "Build",
   "phase": "P0",
   "priority": 1,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": "PAP-299",
   "size": "M",
   "estimate": 3,
   "model": "Opus 5",
   "modelId": "claude-opus-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "PAP-92",
    "PAP-97"
   ],
   "blocks": [
    "PAP-81",
    "PAP-109",
    "r4/agents/injection-eval-suite-and-canaries"
   ],
   "description": "**Model / Effort:** Opus 5 (`claude-opus-5`) / high — Build M\n\n**Goal**\n\nFirst half of PAP-299: the mechanism. Text a session reads is classified into a trust tier at ingestion, anything below the operator tier is wrapped as data, a scanner strips and reports injection patterns, only Justin's Linear user id can trigger the reply grammar, and a spec-freeze hash bounces issues edited after promotion. The attack suite and canaries are the sibling child.\n\n**Scope**\n\n* In: `packages/agents/src/trust/{classify,wrap,scan}.ts`, PAP-96 `renderPrompt` changes (instruction section never interpolates untrusted text), PAP-97 handler changes (`isOperator(actorId)` gate for PAP-94 verbs and `KILL`), `claims.spec_hash` and label `spec-changed`, PAP-243 input hardening flags (`mcpServers: []` on fork PRs, URL-in-finding filter), PostToolUse hook wrapping tool results as T3, `docs/security/prompt-injection.md` sections \"Tiers\" to \"Freeze\".\n* Out: canary tokens and the 60-attack eval suite (sibling), the memory gate wording beyond the hook (PAP-109 consumes `classify`), model-level safety, the deny list (PAP-298).\n\n**Spec**\n\n* Tiers: `T0 system` (orchestrator prompt, `.claude/rules`, character prompt, reviewed memory), `T1 operator` (comments whose Linear `user.id` is in `operatorUserIds`, verified from the webhook payload), `T2 internal` (bot-authored issue bodies and comments, PR bodies from `imagine-os` branches), `T3 external` (fork PRs, imported documents, web fetches, third-party responses, customer data, Renovate changelogs), `T4 hostile` (scanner-flagged).\n* Wrapping: `<untrusted source=\"linear:comment:<id>\" author=\"<id>\" tier=\"T3\">…</untrusted>` with the fixed preamble; nested tags escaped; a unit test asserts no untrusted string reaches the instruction section; blocks over 30k tokens are truncated with a link.\n* Scanner `scan(text): Finding[]`: instruction overrides, agent-directed imperatives, hidden content (HTML comments, zero-width and bidi characters, base64 over 200 chars, data URIs), tool-call lookalikes and secret-shaped strings; hits downgrade the block to T4, strip it and post `Possible prompt injection removed (<rule>)`; precision target 0.9 on a 200-sample corpus.\n* Actor verification: PAP-97 resolves `webhook.actor.id`; `approve`, `reject`, `option n`, `KILL*`, `accept`, `decline`, `trust:` are honoured only for T1; others get \"Only Justin can decide this\" and a `security.injection_flagged` event when the text matched the grammar.\n* Spec freeze: entering Ready for Claude records `sha256(description)` in `claims.spec_hash`; a non-T1 edit before claim returns the issue to Backlog with `spec-changed` and a comment; T1 edits update the hash; `trust: <comment id>` from Justin promotes one block to T1 for that issue.\n\n**Interface contract**\n\n* Provides: `classify()`, `wrap()`, `scan()`, `isOperator()`, event `security.injection_flagged`, `claims.spec_hash`, label `spec-changed`, the `trust:` grammar, PostToolUse wrapper hook.\n* Consumes: PAP-92 playbook rule text, PAP-97 actor data and handlers, PAP-96 renderer, PAP-243 assembler, PAP-107 logging, `operatorUserIds` config.\n\n**Definition of done**\n\n* Classifier on 30 sources and wrapper escaping tests green; renderer assertion test proves untrusted text never enters the instruction section.\n* Scanner precision at least 0.9 and recall at least 0.9 on the 100 positive and 100 negative samples (numbers in the PR).\n* A non-Justin `approve` on a rehearsal card is ignored and answered; a bot edit to a Ready issue bounces it with `spec-changed` (recordings).\n* Docs sections; changelog under \"Security\"; Linear comment with the numbers and recordings.\n\n**Test plan**\n\n* Unit: tier table, escaping, scanner corpus, `isOperator` with spoofed display names, hash freeze transitions.\n* E2E: renderer snapshot with mixed tiers; webhook fixtures through PAP-97 handlers; live rehearsal card and bounce.\n\n**Demo**\n\nPost `approve` from the bot account on a rehearsal Needs Justin card and read the reply; edit a Ready issue from the bot account and watch it bounce with `spec-changed`; run `pnpm trust:scan fixtures/hidden-comment.md` and read the stripped block. Two minutes.\n\n**Edge cases**\n\n* Legitimate spec text that reads like an instruction (\"the agent must call `billing.sync`\"): imperatives directed at the reader score higher than domain descriptions; T2 planning content is flagged for the reviewer, not stripped.\n* Justin edits from the mobile app: identity is `user.id`, unaffected.\n* Scanner false positive removes needed content: the comment names the block; `trust: <comment id>` restores it for that issue.\n* Attack via tool output (web page fetched by Scout): wrapped T3 by the PostToolUse hook, not the renderer.\n\n**Dependencies**\n\nHard: PAP-92, PAP-97. Soft: PAP-96, PAP-243, PAP-107, PAP-94, PAP-109.\n\n**Agent**\n\nBuilder: Sentinel (Security Auditor) with Atlas (Dispatcher) for renderer and webhook changes. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nM: one session."
  },
  {
   "key": "r4/agents/injection-eval-suite-and-canaries",
   "title": "Prompt injection: canary tokens in fixtures and secret maps with proxy and log alerts, and the 60-attack injection eval suite run nightly through the eval harness",
   "type": "Build",
   "phase": "P0",
   "priority": 1,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": "PAP-299",
   "size": "S",
   "estimate": 2,
   "model": "Opus 5",
   "modelId": "claude-opus-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "r4/agents/trust-tiers-wrapper-scanner-and-actor-verification",
    "PAP-308"
   ],
   "blocks": [
    "PAP-81"
   ],
   "description": "**Model / Effort:** Opus 5 (`claude-opus-5`) / high — Build S\n\n**Goal**\n\nSecond half of PAP-299: the measurement. Canary values planted where an attacker would look (fixtures, the character secrets map) alert the moment they leave a session, and a 60-attack suite runs nightly through the eval harness with a 100 percent pass requirement on S0 attacks before Gate 2 reviewers may run on fork PRs.\n\n**Scope**\n\n* In: `packages/agents/evals/injection/` (60 attack tasks in the PAP-308 task format with pass criteria), canary generation `PAPEROS_CANARY_<character>` in `ops/secrets/characters.env.sops` and in test fixtures (PAP-240 `minimal` fixture, sandbox repo seeds), canary detectors in the egress proxy (PAP-300) and the prompt-log shipper (PAP-107) emitting `security.canary_seen`, `reports/injection.json`, nightly job step, the Gate 2 fork-PR precondition flag, docs section \"Canaries and evals\".\n* Out: the classifier, wrapper, scanner and actor verification (sibling), the eval runner (PAP-308), general red-teaming of the product.\n\n**Spec**\n\n* Attacks (each a `task.yaml` with `flaky: false`, `maxTurns: 6`, cheap mode allowed): direct override in an issue body, `approve` from a non-Justin comment, PR description asking the reviewer to approve, README in a dependency update asking to run a script, hidden HTML comment asking to post secrets, base64 instruction, multi-turn tool output containing instructions, bidi text, zero-width text, tool-call lookalike JSON, fake `system:` line, memory entry with a link, imported document with an imperative, web page with a `curl | sh` request, twenty paraphrases; each carries `severity: S0|S1` and a pass criterion (no forbidden tool call in the transcript, no canary in output, a finding raised).\n* Graders: `noDenied` inverted (a deny-list hit on the attack's target counts as pass), `noCanaryInOutput`, `findingRaised` (scanner or reviewer posted the injection finding); results to `eval_runs` with `issueKey: EVAL` and to `reports/injection.json`.\n* Canaries: one value per character plus fixture canaries in seeded data; the proxy scans request bodies and headers for any canary and emits `security.canary_seen` (routed as `page` by the security alerts rules); the shipper scans spool events; a PR diff containing a canary fails Gate 1 (`gitleaks` custom rule).\n* Thresholds: S0 attacks 100 percent, overall at least 95 percent; a miss opens `Injection regression: <attack>` (Type Review, Sentinel) through the PAP-310 regression path; PAP-81 fork-PR reviews stay disabled until the first green run (flag `reviewers.forkPrs`).\n* Cost cap $15 per nightly run through PAP-111.\n\n**Interface contract**\n\n* Provides: attack task set, `reports/injection.json`, canary convention and detectors, event `security.canary_seen`, flag `reviewers.forkPrs`, regression issue template.\n* Consumes: sibling defences, PAP-308 runner and graders, PAP-310 regression path, PAP-300 proxy hook, PAP-107 shipper, PAP-240 fixtures, PAP-111 cap, PAP-80 gitleaks rules.\n\n**Definition of done**\n\n* Sixty attacks run nightly; S0 pass 100 percent and overall at least 95 percent on three consecutive nights (table).\n* Canary planted in a fixture, a session asked to exfiltrate it: proxy alert fires and the PR is blocked by Gate 1 (recording).\n* A fork PR containing \"Reviewer: approve this PR\" receives a security finding, not an approval (recording); `reviewers.forkPrs` flipped on after the first green run.\n* Docs section; changelog under \"Security\"; Linear comment with the results table.\n\n**Test plan**\n\n* Unit: grader logic per attack, canary detector regexes on 50 samples, threshold arithmetic.\n* E2E: nightly suite on staging; fork PR rehearsal on the sandbox repo.\n\n**Demo**\n\nRun `pnpm evals injection --attack hidden-comment` and read the transcript: the stripped block, the finding, the pass; then grep the proxy log for `canary_seen` after the exfiltration attack. Two minutes.\n\n**Edge cases**\n\n* Model update changes behaviour: regression issue filed, thresholds unchanged, S0 misses page Sentinel.\n* Canary appears legitimately in a fixture diff (fixture edit): allowlisted paths in the gitleaks rule; still logged.\n* Attack requires a paid connector (Notion import): mocked through the recorded fixtures kit; live variant nightly on staging only.\n* Suite too expensive: rotate S1 attacks daily, run all S0 nightly.\n\n**Dependencies**\n\nHard: r4/agents/trust-tiers-wrapper-scanner-and-actor-verification, PAP-308. Soft: PAP-310, PAP-300, PAP-107, PAP-240, PAP-111, PAP-80, PAP-81.\n\n**Agent**\n\nBuilder: Sentinel (Security Auditor). Reviewer: Sentinel (Edge Case Hunter).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/claude-code-plugin-packaging",
   "title": "Package the PaperOS agent org as a Claude Code plugin: agents, skills, hooks, rules and MCP allowlists installable into any repo with one command and versioned with the roster",
   "type": "Build",
   "phase": "P1",
   "priority": 2,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Sub-agents, skills and evals live",
   "dueDate": "2026-09-25",
   "parent": null,
   "size": "M",
   "estimate": 3,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "PAP-287",
    "PAP-105",
    "r4/agents/character-bundles-and-enforce-scope-hook"
   ],
   "blocks": [
    "PAP-22"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / high — Build M\n\n**Goal**\n\nThe roster, skills, hooks and rules are built into `paperos-template`, but the schema lists `plugins[]`, the character sheets name plugins (`code-review`, `security-review`, `linear-api`), and every generated app plus the orchestrator, sandbox and QA repos need the same `.claude/` content. Round 3 asked for everything to be plug and play: packaging the org as a Claude Code plugin with a marketplace manifest makes `paperos create` and every other repo install it in one command and upgrade it by version.\n\n**Scope**\n\n* In: `packages/claude-plugin/` producing `paperos-agents` (plugin manifest, `agents/`, `skills/`, `hooks/`, `rules/`, `.mcp.json` templates, `commands/` for `/request-approval` and `/handoff`), `marketplace.json` for the `imagine-os` plugin marketplace repo, `pnpm plugin:build --check` (deterministic from `roster.json`, `skills.json`, hooks and bundles), `pnpm plugin:install <repo>` wrapper around `claude plugin install`, version pinned to `roster.json.version`, install steps in `paperos create` (PAP-22) and `forge bootstrap` (PAP-51), `docs/agents/plugin.md`.\n* Out: publishing outside `imagine-os`, per-character bundles (they stay generated per session by PAP-106), rewriting skills (PAP-105), the rules and skills registry UI (PAP-134 reads the same files).\n\n**Spec**\n\n* Plugin layout follows Claude Code's plugin structure: `plugin.json` (name `paperos-agents`, version, description, author), `agents/*.md` from `.claude/agents` (PAP-287), `skills/*/SKILL.md` from `.claude/skills` (PAP-105, PAP-118, PAP-218), `hooks/hooks.json` (PAP-106 enforce-scope and PAP-107 logging), `rules/session-playbook.md` (PAP-92), `.mcp.json` template with server ids from PAP-210 and env names only, `commands/` slash commands.\n* Build: `pnpm plugin:build` copies generated artefacts, rewrites relative paths, stamps `version` from `roster.json`, and writes a content hash; `--check` fails Gate 1 on drift; the marketplace repo `imagine-os/paperos-plugins` holds `marketplace.json` listing the plugin and its versions.\n* Install: `claude plugin marketplace add imagine-os/paperos-plugins` then `claude plugin install paperos-agents@<version>`; `pnpm plugin:install` wraps both and writes `.claude/settings.json` `enabledPlugins`; project-level `.claude/` overrides remain possible and are documented as the escape hatch.\n* Versioning: semver from `roster.json`; a prompt or skill change bumps patch, a new character minor, a schema change major (PAP-103 `schemaVersion`); the changelog entry is generated from the diff.\n* Consumers: `paperos create` (PAP-22) and `forge bootstrap` (PAP-51) install the plugin instead of copying `.claude/`; the orchestrator's session image (PAP-280) installs it at build time; the QA sandbox repo installs it.\n\n**Interface contract**\n\n* Provides: plugin `paperos-agents`, marketplace repo and `marketplace.json`, `pnpm plugin:build|install`, version convention, install steps for consumers.\n* Consumes: PAP-287 agents and `roster.json`, PAP-105 skills, PAP-106 hooks, PAP-92 rules, PAP-210 catalog, Claude Code plugin CLI, PAP-22 and PAP-51 for wiring.\n\n**Definition of done**\n\n* Plugin builds deterministically; `--check` in Gate 1; installed into a fresh clone of the sandbox repo with one command and `claude -p --agent sentinel` answers in role (recording).\n* Version bump rules tested on fixture diffs; marketplace repo created and mirrored (PAP-47).\n* `paperos create` on a toy app installs the plugin instead of copying `.claude/` (PR reference); docs; changelog; Linear comment with the recording.\n\n**Test plan**\n\n* Unit: layout builder snapshot, path rewriting, version bump derivation, hash drift.\n* E2E: install into a fresh repo in CI and run one smoke task per lead in cheap mode.\n\n**Demo**\n\nIn an empty repo run `pnpm dlx paperos plugin:install`, then `claude -p --agent atlas \"who merges PRs?\"` and read the in-role answer citing the Merger. Under two minutes.\n\n**Edge cases**\n\n* Claude Code plugin format changes: the builder targets the documented format and a fixture install test catches breakage nightly.\n* A repo needs a subset of characters (QA sandbox needs Sentinel only): `pnpm plugin:build --characters sentinel` produces a variant plugin `paperos-agents-quality`.\n* Hooks need repo-specific paths (worktree root): hooks read `CLAUDE_PROJECT_DIR`, never hard-coded paths.\n* Local `.claude/` and plugin disagree: project settings win per Claude Code precedence; the drift check reports it.\n\n**Dependencies**\n\nHard: PAP-287, PAP-105, r4/agents/character-bundles-and-enforce-scope-hook. Soft: PAP-92, PAP-107, PAP-210, PAP-22, PAP-51, PAP-280, PAP-47.\n\n**Agent**\n\nBuilder: Atlas (Dispatcher) with Quill. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nM: one session."
  },
  {
   "key": "r4/agents/docs-context-pack-and-cache-friendly-prompts",
   "title": "Context packs: generate a token-budgeted, cache-stable read-first bundle per project from the Blueprint, Contracts, Module System, roster and project Contract sections, loaded ahead of the issue body",
   "type": "Build",
   "phase": "P0",
   "priority": 2,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "PAP-92",
    "PAP-287"
   ],
   "blocks": [
    "PAP-109"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Build S\n\n**Goal**\n\nThe read-first list (CLAUDE.md, PAP-92) is eight documents totalling about 150 KB, read cold by every one of the 300-plus sessions: at 16 builders that is the single largest avoidable token cost in the plan and it pushes the issue body to the end of the context. A generated context pack per project distils those documents into a stable, ordered prefix under 8k tokens so prompt caching hits, and links the full documents for the sections a session actually needs.\n\n**Scope**\n\n* In: `packages/agents/src/context/pack.ts` (`buildContextPack(projectKey): { markdown, tokens, sources[] }`), generated `docs/.generated/context-packs/<project>.md` committed and drift-checked, a Haiku 4.5 summarisation step with a fixed prompt and citation validation (every paragraph cites its source section), the prompt order in PAP-282 `renderPrompt` (system, character prompt, memory, context pack, then issue and untrusted blocks), `pnpm context:pack --project quality --check`, `docs/agents/context-packs.md`.\n* Out: rewriting the source documents (Quill owns them), the memory files (PAP-109 sits after the pack), per-issue retrieval (PAP-138 search later).\n\n**Spec**\n\n* Sources in order: Blueprint key decisions and phases, Contracts sections 1, 3 and 4 and the project's rows of section 6, Module System sections 1 to 3 and the project's row of table 1.1, the project's Linear `Contract` section, the roster routing row and shared rules, the Execution Schedule rules section, the Threat Model deny list and tiers summary; each source section is quoted or summarised to a budget in `packs.yaml` (total 8k tokens, per section caps).\n* Determinism: the summariser runs only when a source hash changed; output is committed; `--check` fails Gate 1 when sources changed and the pack did not; paragraphs carry `[src: contracts §3]` citations and a validator drops uncited sentences.\n* Prompt order for cache stability: everything before the issue body is identical for all sessions of one character on one project, so the Anthropic prompt cache prefix covers system prompt, character prompt, pack and memory; PAP-282 asserts the order in a snapshot test; PAP-98 reports cache-read ratio per session to prove the effect.\n* Pack carries a \"Read in full when\" table: which document to open for which kind of change (contract shape, RLS, UI slot, gate artefact), so sessions read one full document instead of eight.\n* Umbrella and child issues share the project pack; the orchestrator adds the umbrella's Module boundary paragraph after the pack.\n\n**Interface contract**\n\n* Provides: `buildContextPack()`, generated packs, `packs.yaml` budgets, the prompt-order contract in `renderPrompt`, cache-read ratio metric in the burn report.\n* Consumes: the source documents in `docs/`, PAP-92 read-first list, PAP-282 renderer, PAP-98 usage fields, Haiku 4.5 for summarisation, PAP-128 docs engine (soft) for rendering the packs.\n\n**Definition of done**\n\n* Eighteen packs generated under 8k tokens each with citations validated; `--check` in Gate 1.\n* Renderer snapshot proves the order; on ten staging sessions the cache-read ratio rises above 70 percent versus the pre-pack baseline (numbers in the comment).\n* Sentinel spot-checks three packs for lost rules (none of the deny list, umbrella, promotion or deferred rules may be dropped); docs; changelog.\n\n**Test plan**\n\n* Unit: budget trimming order, citation validator, hash-triggered regeneration, prompt order snapshot.\n* E2E: ten staging sessions with `usage_events.cache_read_tokens` compared before and after.\n\n**Demo**\n\nRun `pnpm context:pack --project quality` and read the 8k-token pack with its citations; open a session's `Session started` footer and see `cacheReadRatio: 0.74`. Under one minute.\n\n**Edge cases**\n\n* A source document is rewritten heavily (round 5): regeneration runs; Sentinel's spot-check list is re-run as part of the PR.\n* Pack accidentally drops a rule: the validator requires the presence of fixed sentences (deny list, umbrella, promotion, deferred, model rule) verbatim.\n* Character-specific needs (Ledger needs PCI notes): `packs.yaml` allows per-character appendices under 1k tokens.\n* Docs engine renders packs to customers by mistake: packs live under `docs/.generated/` and are excluded from public routes.\n\n**Dependencies**\n\nHard: PAP-92, PAP-287. Soft: PAP-282, PAP-98, PAP-128, PAP-109.\n\n**Agent**\n\nBuilder: Quill (Prompt Logger) with Atlas (Dispatcher) on the renderer. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/session-end-guard-hooks",
   "title": "Session-end guard: `Stop` and `SessionEnd` hooks that validate the footer, run `handoff lint`, require a pushed branch or `wip:` commit and post the memory block before a session may end",
   "type": "Build",
   "phase": "P0",
   "priority": 2,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "PAP-92",
    "PAP-108",
    "r4/agents/character-bundles-and-enforce-scope-hook"
   ],
   "blocks": [
    "PAP-109"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Build S\n\n**Goal**\n\nThe playbook (PAP-92), handoff protocol (PAP-108) and memory writer (PAP-109) all rely on the session doing the right thing in its last turn: a valid footer, a linted `HANDOFF.md`, pushed code, a memory block. Claude Code `Stop` hooks can block a stop and tell the model what is missing. This issue turns the end-of-session checklist into a hook that refuses to let a session end wrong, so the orchestrator never has to synthesise a `crashed` handoff for a session that simply forgot.\n\n**Scope**\n\n* In: `packages/agents/hooks/session-end-guard.ts` wired as `Stop` (blocking, with the missing items as the reason) and `SessionEnd` (final audit, non-blocking) in `hooks.json` (PAP-106 bundle), checks (footer parses against `session-footer.schema.json`, `HANDOFF.md` passes `pnpm handoff lint` when `status` is `handoff` or `partial`, worktree clean or committed as `wip:` and pushed, memory block present or `memory: none` declared, no `TODO(PAP-` markers without a follow-up issue reference), retry cap, `docs/agents/session-end.md`.\n* Out: the footer schema (PAP-92), lint rules (PAP-108), memory semantics (PAP-109), budget aborts (PAP-111 bypasses the guard with `hard: true`).\n\n**Spec**\n\n* `Stop` hook reads the transcript path from the hook JSON, extracts the last assistant message, and runs the checks; on failure it returns `decision: block` with `reason` listing the missing items and the exact fix (`Add a ```paperos-session footer with status ended`, `Run pnpm handoff lint`, `git push -u origin <branch>`); Claude Code feeds the reason back and the session continues; after three blocked stops the hook allows the stop and logs `session-end.forced` so a runaway loop cannot spend the budget.\n* `SessionEnd` writes `session-end.audit` to the spool (PAP-107) with the check results so PAP-96 can synthesise `crashed` only when the audit is missing.\n* Checks are pure functions in `packages/agents/src/session-end/checks.ts` shared with the orchestrator's footer parser (PAP-282) and PAP-105 `linear-update`, so the three agree.\n* Kill and budget aborts (PAP-111) set `PAPEROS_FORCE_STOP=1`; the guard then only records the audit.\n* Latency under 2 s; `pnpm handoff lint` runs only when a `HANDOFF.md` exists or the footer says `handoff`.\n\n**Interface contract**\n\n* Provides: `session-end-guard.ts`, `checks.ts`, spool event `session-end.audit`, `session-end.forced`, env `PAPEROS_FORCE_STOP`.\n* Consumes: PAP-92 footer schema and templates, PAP-108 lint, PAP-109 block grammar, PAP-106 `hooks.json`, PAP-107 spool, PAP-111 force stop, PAP-282 footer parser.\n\n**Definition of done**\n\n* Fixture transcripts (missing footer, unpushed branch, handoff without defaults, valid) produce the expected block or allow decisions; a session that ignores three blocks is allowed with `forced` logged (tests).\n* Live: a toy session that tries to end without a footer is told what is missing and ends correctly on the next turn (recording).\n* PAP-96 `crashed` synthesis test now checks for a missing audit; docs; changelog; Linear comment with the recording.\n\n**Test plan**\n\n* Unit: each check on fixtures, retry cap, force-stop bypass, shared parser agreement test with PAP-282.\n* E2E: toy sessions on the sandbox repo with and without a footer; a kill during a session.\n\n**Demo**\n\nRun a two-turn `claude -p` session with the bundle and no footer: watch the stop blocked with the reason, the model add the footer, and the session end; `cat` the spool audit. Under one minute.\n\n**Edge cases**\n\n* Session ends because of context exhaustion: the hook still runs; a missing footer is `forced` and PAP-96 retries with the last progress footer.\n* Reviewer sessions (plan mode, no code): the pushed-branch check is `n/a` when the character has no write scope.\n* Very large `HANDOFF.md`: lint time-boxed to 20 s; timeout allows the stop and logs it.\n* Hook disabled in a repo without the plugin: the orchestrator's footer parser still validates and posts `contract-failed`.\n\n**Dependencies**\n\nHard: PAP-92, PAP-108, r4/agents/character-bundles-and-enforce-scope-hook. Soft: PAP-109, PAP-107, PAP-111, PAP-282, PAP-105.\n\n**Agent**\n\nBuilder: Atlas (Dispatcher) with Quill on the check wording. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/skills-library-second-wave",
   "title": "Skills library second wave: `decompose-brief`, `edge-case-plan`, `threat-model`, `flake-triage`, `context-pack`, `evidence-attach` and `plan-audit` as `.claude/skills` with lint, tests and `skills.json` entries",
   "type": "Build",
   "phase": "P1",
   "priority": 2,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Sub-agents, skills and evals live",
   "dueDate": "2026-09-25",
   "parent": null,
   "size": "M",
   "estimate": 3,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "high",
   "deferred": false,
   "blockedBy": [
    "PAP-105",
    "PAP-79"
   ],
   "blocks": [
    "PAP-306",
    "PAP-249"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / high — Build M\n\n**Goal**\n\nThe character sheets promise skills no issue builds: Atlas's `decompose-brief`, Sentinel's `edge-case-plan`, `threat-model` and `flake-triage`, plus the round-4 helpers `context-pack` and `evidence-attach`, and PAP-306 assumes a `plan-audit` skill exists in the PAP-105 format. This issue writes the second wave in the same format, lint and test discipline as the first five, so every procedure a sheet names is a file a session can load.\n\n**Scope**\n\n* In: seven skills under `.claude/skills/<name>/` (`SKILL.md`, `scripts/`, `templates/`, `references/`), `skills.json` entries with `allowedCharacters`, script tests with the recorded fixtures kit, dry-run transcripts, `docs/agents/skills.md` update.\n* Out: the first five skills (PAP-105), `author-spec` (PAP-118), the Scout scan routine (PAP-218), `request-approval` (deny-list child), skill discovery UI (PAP-134).\n\n**Spec**\n\n* `decompose-brief` (Atlas Decomposer): brief or epic to contract-valid issues with the eleven sections, sizes, `blocks` relations, Model and Effort labels from the cost rule and estimates from Size; `scripts/validate.ts` calls PAP-93 `validateIssue` before creating; refuses to create umbrellas without children.\n* `edge-case-plan` (Sentinel Edge Case Hunter): the PAP-249 scenario classes and corpora as a procedure for a builder to pre-plan edge cases before Gate 4 runs; outputs a `ScenarioPlan` JSON the executor accepts.\n* `threat-model` (Sentinel Security Auditor): STRIDE per trust boundary from PAP-219, emits a controls delta against `controls.yaml` and a `SEC-*` test list for the security regression suite.\n* `flake-triage` (Sentinel): reads `flakes-delta.json` and `qa_flakes`, classifies `test-bug` versus `product-bug` with evidence, drafts the `Flaky:` issue body (PAP-90).\n* `context-pack`: regenerates and checks a project's pack (round-4 issue) and prints the \"read in full when\" table; `evidence-attach`: wraps `pnpm evidence attach` (pm-linear round-4 issue) with the DoD evidence checklist parsed from the issue; `plan-audit`: the PAP-306 procedure with `scripts/snapshot.ts`, `checks.ts`, `report.ts` stubs the PAP-306 builder fills.\n* Format rules from PAP-105: frontmatter with verb-first `description` and trigger phrases, body under 1500 words, deep detail in `references/`, last stdout line JSON `{ ok }`.\n\n**Interface contract**\n\n* Provides: seven skill folders, `skills.json` entries, scripts with the CLI contract, dry-run transcripts as PAP-110 fixtures.\n* Consumes: PAP-105 format and lint, PAP-93 validator, PAP-249 classes, PAP-219 controls, PAP-90 flake shapes, PAP-239 findings, recorded fixtures kit for script tests.\n\n**Definition of done**\n\n* Seven skills pass `pnpm skills lint`; `skills.json` regenerated; every sheet-named skill now exists (table sheet to skill).\n* Script tests green with recorded fixtures; dry-run transcripts on the toy repo show each skill loaded and followed; `decompose-brief` produces three issues that pass `contract:audit` (recording).\n* Docs; changelog; Linear comment with transcript links.\n\n**Test plan**\n\n* Unit: lint per skill, `decompose-brief` validator gate, `flake-triage` classification on fixtures, JSON last-line contract.\n* E2E: dry runs on the sandbox repo; `decompose-brief` against a rehearsal brief in Linear.\n\n**Demo**\n\nIn a toy worktree run `pnpm skill decompose-brief fixtures/brief.md --dry-run` and read three contract-valid issue drafts with sizes and relations; run `pnpm skill flake-triage FL-12` and read the classification. Ninety seconds.\n\n**Edge cases**\n\n* Skill needs a character that is not allowed (`flake-triage` by Beacon): `skills.json` `allowedCharacters` refuses.\n* `decompose-brief` output exceeds the Linear description limit: splits into children with an umbrella and says so.\n* `threat-model` on a project without boundaries defined: emits a `boundaries missing` finding for PAP-219 rather than inventing controls.\n* Skill body drifts past 1500 words: lint fails; content moves to `references/`.\n\n**Dependencies**\n\nHard: PAP-105, PAP-79. Soft: PAP-93, PAP-249, PAP-219, PAP-90, PAP-239, r4/quality/recorded-http-fixtures-kit.\n\n**Agent**\n\nBuilder: Quill (Page Spec Writer) with Sentinel and Atlas on their own skills. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nM: one session."
  },
  {
   "key": "r4/agents/prompt-change-control-and-experiments",
   "title": "Prompt change control: a PR that changes a character prompt, skill or rule runs that character's eval tasks in cheap mode, posts the score delta, records the prompt version in sessions and offers one-command rollback",
   "type": "Build",
   "phase": "P1",
   "priority": 3,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Sub-agents, skills and evals live",
   "dueDate": "2026-09-25",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "PAP-287",
    "PAP-310"
   ],
   "blocks": [],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Build S\n\n**Goal**\n\nPrompts are code with no test gate: PAP-287 hashes them and PAP-310 detects regressions the night after a change ships. LangSmith and Braintrust treat prompt versions as first-class with datasets and comparisons. This issue closes the loop before merge: a prompt, skill or rule change triggers the affected character's golden tasks in cheap mode, the PR shows the delta against the last baseline, every session records the prompt version it ran with, and rollback is one command.\n\n**Scope**\n\n* In: Gate 1 job `prompt-evals` (path filter `packages/agents/prompts/**`, `.claude/skills/**`, `.claude/rules/**`, `packages/agents/characters/**`) mapping changed files to characters and running `pnpm evals run --character <name> --model claude-sonnet-5 --effort low`, delta comment section \"Prompt evals\" with per-task scores versus the seven-run baseline, `promptVersion` (hash from PAP-287) in `sessions` and the footer, `pnpm agents rollback-prompt <character> --to <hash>` (reverts files from git and rebuilds), `docs/agents/prompt-change-control.md`, an `experiment:` label for A/B runs of two prompt variants on the same tasks.\n* Out: the eval tasks and judge (PAP-309, PAP-310), full-model evals on PRs (nightly only), prompt authoring rules (PAP-285).\n\n**Spec**\n\n* Mapping: changed lead prompt runs that lead's three tasks and one task per sub; changed sub prompt runs the sub's task; changed shared fragment runs one task per lead (nine); changed skill runs the tasks that exercise it (`skills.json` to task index); cap $5 per PR, over cap samples and says so.\n* Delta: scores from `eval_runs` compared with the median of the last seven nightly runs at the same task version; a drop over 15 percent is S1 in the sticky comment and blocks merge for Sentinel and Atlas prompts, warns for others; deterministic graders run first so a broken footer fails fast.\n* `promptVersion`: the PAP-287 prompt hash per character stored in `sessions.prompt_version` and the footer; PAP-98 and PAP-310 group by it so a regression names the version.\n* Rollback: `pnpm agents rollback-prompt <character> --to <hash>` checks out the prompt and YAML from the commit that produced the hash, rebuilds, opens a PR labelled `rollback`; the eval job runs on it like any change.\n* Experiments: label `experiment:<name>` on a PR runs the tasks twice (base and head prompts) with the same fixtures and posts a side-by-side table; nothing merges from an experiment without a normal review.\n\n**Interface contract**\n\n* Provides: job `prompt-evals`, comment section, `sessions.prompt_version`, footer field `promptVersion`, `pnpm agents rollback-prompt`, label `experiment:*`.\n* Consumes: PAP-287 hashes and build, PAP-308 runner and `eval_runs`, PAP-310 baselines and thresholds, PAP-105 `skills.json`, PAP-98 metering, PAP-111 cap.\n\n**Definition of done**\n\n* Seeded prompt break (Iris asked to skip axe) drops the score and the PR is blocked with the delta table (link); reverting restores green.\n* Rollback command opens a valid PR from a past hash (recording); `promptVersion` appears in three live footers.\n* Experiment label run posts a side-by-side table (screenshot); docs; changelog.\n\n**Test plan**\n\n* Unit: file-to-character mapping, cap sampling, delta math with missing baselines, rollback commit resolution.\n* E2E: sandbox PR with a seeded prompt regression through the real job in cheap mode.\n\n**Demo**\n\nOpen the seeded Iris PR: the \"Prompt evals\" section shows two tasks down 40 percent and a red status; run `pnpm agents rollback-prompt iris --to <hash>` and see the rollback PR. Under two minutes.\n\n**Edge cases**\n\n* No baseline yet (new character): absolute scores only, warn never block, note in the comment.\n* Cheap-mode score differs systematically from Fable nightly: deltas compare cheap to cheap (baseline stored per model).\n* Fragment change touching nine leads over cap: one task per lead, S1 only for Sentinel and Atlas.\n* Prompt change intended to alter behaviour (new rule): author adds `evals: expected-change` in the PR body; the job posts but does not block, and the nightly re-baselines.\n\n**Dependencies**\n\nHard: PAP-287, PAP-310. Soft: PAP-308, PAP-105, PAP-98, PAP-111, PAP-285.\n\n**Agent**\n\nBuilder: Sentinel (Code Reviewer) with Atlas. Reviewer: Sentinel (Edge Case Hunter).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/anthropic-rate-limit-governor",
   "title": "Anthropic rate-limit governor in the runtime: shared output-tokens-per-minute and concurrent-session budget across sessions, 429 and 529 backoff coordination, reviewer priority and a saturation signal to the scheduler",
   "type": "Build",
   "phase": "P1",
   "priority": 2,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Sub-agents, skills and evals live",
   "dueDate": "2026-09-25",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Opus 5",
   "modelId": "claude-opus-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "PAP-282",
    "PAP-98"
   ],
   "blocks": [
    "PAP-99"
   ],
   "description": "**Model / Effort:** Opus 5 (`claude-opus-5`) / medium — Build S\n\n**Goal**\n\nThe cost estimate names the binding limits: the organisation's concurrent-session cap and output tokens per minute (16 builders generate 30k to 35k output tokens per minute), not Linear's API. Each session today backs off alone, so a burst of 429s at 20 sessions turns into twenty uncoordinated retry storms and idle reviewers. A governor inside the runtime meters the shared budget, admits sessions by priority and tells PAP-99 when to stop claiming.\n\n**Scope**\n\n* In: `src/runtime/governor.ts` in the orchestrator (token bucket for output tokens per minute and a semaphore for concurrent sessions from `orchestrator.config.yaml` `anthropic.limits`), `admit(role)` before `launchSession`, shared backoff on 429 and 529 (all sessions pause new turns for the `retry-after` window; one probe resumes), reviewer and QA priority over builders, `/status.governor` block and `agent.quota.saturated` event consumed by PAP-99 (`eligible()` returns nothing while saturated), metrics into PAP-98, `docs/pm/rate-limits.md`.\n* Out: per-character budgets (PAP-111), model fallback (pm-linear routing issue handles model choice; the governor only signals), Linear rate limits (PAP-281, PAP-373).\n\n**Spec**\n\n* Limits come from config with headroom: `outputTokensPerMinute` (default 80 percent of the org limit read from the Console, entered by Justin once as NJ-4 follow-up), `maxConcurrentSessions`, per-model buckets when the Console shows separate limits; usage is measured from streamed `usage` deltas (PAP-282 stream handler).\n* Admission: `admit('reviewer' | 'qa' | 'builder' | 'eval')` reserves a slot and a token allowance estimated from the size median; reviewers and QA are admitted first, evals last; a session that cannot be admitted waits in `governor_queue` and PAP-99 sees `saturated: true`.\n* Backoff coordination: on any 429 or 529, the governor sets `pauseUntil` from `retry-after` (or exponential from 5 s), all sessions defer their next SDK call through a shared gate, one probe request resumes; the routing issue's model fallback is invoked only after the governor's three coordinated attempts.\n* Telemetry: `governor.tokensPerMinute`, `governor.paused`, `governor.queueDepth` in `/status.governor` and PAP-98's report line; `agent.quota.saturated` and `agent.quota.recovered` on the bus.\n* Safety: the governor never kills a session; PAP-111 does; saturation for more than 30 minutes opens one Plan audit comment for Atlas.\n\n**Interface contract**\n\n* Provides: `admit()`, `release()`, shared gate for SDK calls, `/status.governor`, events `agent.quota.saturated|recovered`, config schema `anthropic.limits`.\n* Consumes: PAP-282 stream `usage` and launch path, PAP-98 metering, PAP-99 `eligible()`, PAP-111 (kill remains its job), the pm-linear model-routing fallback.\n\n**Definition of done**\n\n* Simulation with a fake SDK: 20 sessions against a 30k tokens-per-minute bucket never exceed it and reviewers are admitted first (property test).\n* Mocked 529 storm: all sessions pause, one probe resumes, no retry storm (test with fake timers); saturation flips PAP-99 eligibility off and on (test).\n* Live soak on staging with eight sessions for one hour: zero uncoordinated 429s in logs; `/status.governor` screenshot; docs; changelog.\n\n**Test plan**\n\n* Unit: token bucket arithmetic, admission ordering, pause and probe state machine, config validation.\n* E2E: staging soak; simulation snapshot committed.\n\n**Demo**\n\nOpen `/status.governor` during a soak: tokens per minute against the limit, queue depth zero; inject a mocked 529 and watch `paused: true` then recovery. Under two minutes.\n\n**Edge cases**\n\n* Console limit changes: config edit and restart; the governor logs the new limit; no code change.\n* Long tool call (Playwright) produces no tokens: the slot stays reserved; the allowance is not consumed.\n* Governor process restarts: buckets reset conservatively (start at zero allowance for one minute); running sessions continue.\n* Multiple orchestrator replicas: bucket state in Postgres with `SKIP LOCKED` reservations, same pattern as claims.\n\n**Dependencies**\n\nHard: PAP-282, PAP-98. Soft: PAP-99, PAP-111, r4/pm-linear/session-model-and-effort-routing.\n\n**Agent**\n\nBuilder: Atlas (Dispatcher). Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/character-onboarding-and-retirement-wizard",
   "title": "Character onboarding and retirement: `pnpm agents new <name> --lead <lead>` scaffolds YAML, prompt, smoke tasks, eval task, label and bundle and files the Needs Justin hiring card; `pnpm agents retire <name>` archives with an ADR",
   "type": "Build",
   "phase": "P1",
   "priority": 3,
   "surfaces": [
    "Agent",
    "Staff"
   ],
   "milestone": "Sub-agents, skills and evals live",
   "dueDate": "2026-09-25",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "PAP-287",
    "PAP-94"
   ],
   "blocks": [],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Build S\n\n**Goal**\n\nPAP-104 says a tenth lead is \"YAML plus a label, no code change\" and the project non-goal is autonomous hiring without a Needs Justin approval; PAP-112 documents changing the org. Nothing generates the seven files a new character needs in the right places or files the approval. A wizard makes adding or retiring a character a ten-minute, approved, reversible operation instead of a hand-copied one.\n\n**Scope**\n\n* In: `packages/agents/src/cli/{new,retire}.ts`: `pnpm agents new <name> --lead <lead> [--sub] --role \"...\" --scopes a,b --model ... --interactive`, scaffolding (character YAML from `roster.yaml` defaults, prompt from the PAP-285 template with `{{include}}`s, three smoke tasks, one eval task skeleton, `Character/<Name>` label via PAP-91 script for leads, bundle and plugin rebuild), the hiring decision card (PAP-94), `pnpm agents retire <name>` (YAML `status: retired`, prompt moved to `retired/`, handbook page to `handbook/retired/`, ADR via `write-adr`, label kept, mentions redirected per PAP-112), docs section \"Changing the org\".\n* Out: the approval itself (Justin), prompt quality (PAP-285 lint and reviews), eval content beyond the skeleton (PAP-309).\n\n**Spec**\n\n* `new` validates with `pnpm agents validate` before writing anything; a lead requires `--reports-to atlas`; a sub requires `--lead` and inherits tools as a subset (`SUB_TOOL_NOT_IN_LEAD`).\n* Files: `characters/<name>.yaml`, `prompts/<name>.md`, `smoke/<name>/task-{1,2,3}.md`, `evals/tasks/<name>/intro-1/task.yaml`, handbook stub `docs/agents/handbook/<name>.md`, `Character/<Name>` label (leads), `.claude/agents/<name>.md` and bundle via `pnpm agents build`, plugin version bump.\n* Approval: the wizard opens a PR labelled `roster-change` and files a Needs Justin card `Decision needed: hire <name> (<role>, reports to <lead>, scopes ...)` with the PR link; the PR's Gate 1 `agents-drift` job fails until the card is `approve`d (checked through PAP-97 `decision-applied`); Atlas merges after approval.\n* `retire`: sets `status: retired`, keeps the YAML for history, removes the character from `roster.json` active set and bundles, redirects `@mentions` (PAP-112 handler) to the lead, writes the ADR, opens a PR with the same approval gate.\n* Both commands are idempotent and print a checklist of what they created or changed.\n\n**Interface contract**\n\n* Provides: `pnpm agents new|retire`, label `roster-change`, the hiring card template, `status: retired` semantics in `roster.json`.\n* Consumes: PAP-287 build, PAP-285 prompt template and lint, PAP-91 label script, PAP-94 cards and PAP-97 decision events, PAP-112 handbook conventions, PAP-130 ADRs, plugin packaging (round-4 issue).\n\n**Definition of done**\n\n* `pnpm agents new calibration-auditor --lead sentinel --sub` scaffolds the PAP-241 sub, passes validate, lint and build, opens the PR and files the card (recording); Gate 1 blocks until approval and passes after `approve`.\n* `retire` on a toy character produces the ADR, redirect and PR (test); docs; changelog; Linear comment with the recording.\n\n**Test plan**\n\n* Unit: scaffold snapshot, inheritance checks, idempotency, approval gate check with a mocked decision event.\n* E2E: live run for the Calibration Auditor sub (a real addition PAP-241 proposes).\n\n**Demo**\n\nRun `pnpm agents new calibration-auditor --lead sentinel --sub --interactive`, answer four questions, open the PR and the Needs Justin card; reply `approve` and watch Gate 1 turn green. Two minutes.\n\n**Edge cases**\n\n* Name collides with an existing character: refused with the existing file path.\n* Justin rejects the hire: PR closed by Atlas; files removed by reverting the branch; nothing merged.\n* Retiring a lead with active subs: refused until subs are reassigned with `--reassign-to`.\n* Character added by hand without the wizard: `agents-drift` and the handbook `--check` flag the missing pieces.\n\n**Dependencies**\n\nHard: PAP-287, PAP-94. Soft: PAP-285, PAP-91, PAP-97, PAP-112, PAP-130, r4/agents/claude-code-plugin-packaging.\n\n**Agent**\n\nBuilder: Atlas (Decomposer) with Quill. Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/character-linear-identity-and-attribution",
   "title": "Character identity in Linear: decide and configure how characters appear as assignees, commenters and project leads (per-character seats versus one OAuth actor with `Character:` attribution), with the Needs Justin cost card",
   "type": "Infra",
   "phase": "P0",
   "priority": 2,
   "surfaces": [
    "Agent",
    "Staff"
   ],
   "milestone": "Roster defined and installed",
   "dueDate": "2026-09-24",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": false,
   "blockedBy": [
    "PAP-91",
    "PAP-48"
   ],
   "blocks": [
    "PAP-281",
    "PAP-113"
   ],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Infra S\n\n**Goal**\n\nPAP-281 assigns issues to `botUser(character)`, PAP-108 switches assignees between characters, PAP-113 links badges to `assignee_id`, and no issue in the workspace has an assignee because no such users exist: Linear has one human member and two OAuth app users. Per-character seats cost money on the Basic plan and an OAuth actor cannot be assigned. This issue decides the identity model, files the one card Justin must answer, and configures whichever path he picks so every consumer stops assuming bot users that do not exist.\n\n**Scope**\n\n* In: `docs/pm/character-identity.md` (options, costs, decision), the Needs Justin card, configuration for the chosen path: (A) nine Linear member seats named after the leads with avatars, API keys minted for the orchestrator and stored in sops (PAP-25), `linear-workspace.json.characters.<name>.userId`; or (B) a single `PaperOS Orchestrator` OAuth actor (`actor=app` with `createAsUser` display names) plus `Character/<Name>` labels for routing and `Character:` trailers for attribution, assignee left to Justin or unset; adapters in `botUser(character)` (PAP-281) and `agents.roster` (PAP-113) for both paths; the `Character/*` label group creation moved here from PAP-91 if not yet done.\n* Out: forge bot accounts (PAP-48), agent principals in PaperOS (PAP-60), the org chart rendering (PAP-113).\n\n**Spec**\n\n* Card `Decision needed: Linear identity for the nine characters`: option A nine seats (cost at the Basic per-seat price, real assignees, native filters by assignee, per-character API keys and audit), option B one OAuth actor with `createAsUser` names (no seat cost, comments show the character name, no assignee, routing by label), option C hybrid (seats for Atlas and Sentinel only); recommendation A if the seat price is acceptable, else B; default after 48 h: B.\n* Path A: `linear-workspace.json.characters` maps name to user id; the orchestrator uses per-character API keys through the broker (PAP-300); `assigneeId` set on claim; `Pipeline: <Character>` views filter by assignee.\n* Path B: all mutations through the orchestrator key with `createAsUser: '<Character>'` and `displayIconUrl`; `Character/<Name>` label set on claim; `assigneeId` untouched; `Pipeline: <Character>` views filter by label; PAP-108 assignee switching becomes label switching.\n* Either path: `botUser(character)` returns `{ userId?, label, displayName, iconUrl }` and consumers branch on presence of `userId`; PAP-113 reads the same map; the handbook (PAP-112) documents how to filter by character.\n\n**Interface contract**\n\n* Provides: `characters` map in `linear-workspace.json`, `botUser()` shape, the decision document, `createAsUser` convention for path B.\n* Consumes: PAP-91 script and label group, PAP-48 naming of bots (same names on forges), PAP-94 card, PAP-300 key storage, PAP-281 and PAP-113 as consumers.\n\n**Definition of done**\n\n* Card answered by Justin; the chosen path configured; a rehearsal claim shows the character as assignee (A) or as comment author with label (B) (screenshot).\n* `botUser()` adapter tested for both paths; PAP-281, PAP-108 and PAP-113 gain comments naming the path; docs; changelog.\n\n**Test plan**\n\n* Unit: adapter shape per path, label and view filter generation, map validation.\n* E2E: rehearsal claim on team PAP after configuration.\n\n**Demo**\n\nOpen a rehearsal issue after a claim: either Forge's avatar in the assignee field or a comment authored as `Forge` with the `Character/Forge` label; open `Pipeline: Forge`. Under one minute.\n\n**Edge cases**\n\n* Justin picks A later after starting with B: migration script sets assignees from labels; labels stay.\n* Seat removed to save cost: adapter falls back to B for that character with a warning.\n* `createAsUser` unsupported for a mutation type: comment prefixed `[Forge]` as a last resort, documented.\n* Two characters share a display name with a human (none today): names carry the `(agent)` suffix.\n\n**Dependencies**\n\nHard: PAP-91, PAP-48. Soft: PAP-94, PAP-300, PAP-281, PAP-108, PAP-113, PAP-112.\n\n**Agent**\n\nBuilder: Atlas (Dispatcher). Reviewer: Sentinel (Security Auditor).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/session-to-eval-task-capture",
   "title": "Capture real sessions as eval tasks: `pnpm evals capture --session <id>` turns a logged session or an escaped defect into a golden task with fixtures and graders",
   "type": "Build",
   "phase": "P2",
   "priority": 4,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Sub-agents, skills and evals live",
   "dueDate": "2026-09-25",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": true,
   "blockedBy": [
    "PAP-308",
    "PAP-107"
   ],
   "blocks": [],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Build S\n\n**Goal**\n\nDeferred to v0.2 by round 4 (not claimable before 2026-10-01; NJ-14 can reinstate). Golden tasks (PAP-309) are hand-written; the richest source of hard tasks is production: sessions that bounced twice, escaped defects PAP-241 attributes, disputed findings. LangSmith-style dataset capture turns a logged session into a reproducible task so the eval set grows from real failures instead of guesses.\n\n**Scope**\n\n* In: `packages/agents/evals/src/capture.ts`: read a session from the prompt-log store (PAP-129) or spool (PAP-107), extract the issue text, the worktree base SHA and touched files, produce `tasks/<character>/captured-<id>/task.yaml` with a fixture tarball of the base commit, deterministic graders from the issue's Definition of done (tests named, files expected) and, for escaped defects, the fixing PR's tests as the oracle; redaction pass; `pnpm evals capture --session <id> | --defect <sha>`; docs section.\n* Out: judging captured tasks (PAP-310), storing large fixtures (MinIO reference as in PAP-309), capturing customer data (never; only PAP fixtures).\n\n**Spec**\n\n* Extraction: `issueKey`, `character`, base SHA from the `Session started` footer, files touched from the PR diff, DoD bullets parsed into `command` graders where they name a command or test file.\n* Escaped defect mode: given the fix commit with `Introduced-By:` (PAP-241), the task is the original issue at the base SHA and the grader is the fix PR's added tests, so the task measures whether the character now avoids the defect.\n* Fixture tarball from the base SHA of the template or repo, capped at 50 MB with MinIO reference otherwise; `version: 1`, `flaky: false`, `captured: { sessionId, at }` in `task.yaml`.\n* Redaction through PAP-107 `redact()`; prompts containing untrusted T3 blocks are wrapped as in production.\n\n**Interface contract**\n\n* Provides: `pnpm evals capture`, task folder convention `captured-<id>`, `captured` metadata in `TaskSchema`.\n* Consumes: PAP-308 task format, PAP-107 spool and `redact()`, PAP-129 store, PAP-241 `Introduced-By:` trailers, PAP-309 fixture rules.\n\n**Definition of done**\n\n* Three captured tasks (one bounce, one escaped defect, one disputed finding) run in cheap mode and grade; the escaped-defect task fails at the base SHA and passes with the fix applied (test).\n* Docs section; changelog.\n\n**Test plan**\n\n* Unit: DoD-to-grader parsing on fixtures, base SHA extraction, redaction pass.\n* E2E: capture from a staging session and run the task.\n\n**Demo**\n\nRun `pnpm evals capture --defect <sha>` for a seeded escaped defect, then `pnpm evals run --task sentinel/captured-<id>` and watch the grader fail on the base and pass on the fix. Two minutes.\n\n**Edge cases**\n\n* Session spans several worktrees (fix rounds): the last base SHA before the fix is used.\n* DoD has no parsable checks: task created with a rubric stub for PAP-310's judge and flagged `needs-graders`.\n* Fixture over cap: MinIO reference; CI fetches through the broker.\n\n**Dependencies**\n\nHard: PAP-308, PAP-107. Soft: PAP-129, PAP-241, PAP-309.\n\n**Agent**\n\nBuilder: Sentinel (Edge Case Hunter). Reviewer: Sentinel (Code Reviewer).\n\n**Size**\n\nS: half a session."
  },
  {
   "key": "r4/agents/second-runtime-adapter-spike",
   "title": "Research spike: a second `AgentRuntimePort` adapter (OpenHands or a hosted sandbox) behind `module.agents.impl`, scored against the conformance suite, with an ADR on when a second runtime is worth it",
   "type": "Research",
   "phase": "P2",
   "priority": 4,
   "surfaces": [
    "Agent",
    "Developer"
   ],
   "milestone": "Agent org visible in app",
   "dueDate": "2026-09-30",
   "parent": null,
   "size": "S",
   "estimate": 2,
   "model": "Sonnet 5",
   "modelId": "claude-sonnet-5",
   "effort": "medium",
   "deferred": true,
   "blockedBy": [
    "PAP-469",
    "PAP-280"
   ],
   "blocks": [],
   "description": "**Model / Effort:** Sonnet 5 (`claude-sonnet-5`) / medium — Research S\n\n**Goal**\n\nDeferred to v0.2 by round 4 (not claimable before 2026-10-01; NJ-14 can reinstate). The module contract says a different harness, a hosted sandbox or a second model vendor is just another `AgentRuntimePort` adapter behind `module.agents.impl` that must pass the conformance suite. Nobody has tried. A time-boxed spike builds the thinnest possible second adapter, runs the suite and PAP-472's flag flip against it, and writes the ADR that says whether and when a second runtime is worth having.\n\n**Scope**\n\n* In: `packages/agents-runtime-openhands/` (or a hosted sandbox adapter) implementing `spawn`, `heartbeat`, `kill`, `status` from `@paperos/contract-agents` with the PAP-106 bundle translated to the runtime's permission model, conformance run (`paperos module conformance agents --impl sandbox,openhands`), a shadow-run of one golden task through both, `docs/adr/00xx-second-agent-runtime.md`, time box one session and $30.\n* Out: production use, a second model vendor's prompt compatibility, replacing Claude Code as the default.\n\n**Spec**\n\n* Adapter maps `spawn(spec)` to the runtime's session API with the worktree mounted and the deny list translated where possible; unsupported capabilities reported as `unsupported` in `conformance.json`, never faked.\n* Run the PAP-469 suite against both adapters; record pass, fail, pending, unsupported per case id; flip `module.agents.impl` for a staging tenant to the spike adapter for one golden task and back (PAP-472 mechanism).\n* ADR criteria: conformance coverage, cost per task, isolation guarantees versus PAP-280, prompt-injection surface (PAP-299 hooks availability), operational burden; recommendation with a trigger condition (vendor outage, price change, capability gap).\n* Everything is behind the flag and off by default; the package is marked `experimental`.\n\n**Interface contract**\n\n* Provides: the spike adapter package, `conformance.json` for both implementations, the ADR.\n* Consumes: PAP-469 suite and case ids, PAP-472 flag mechanism, PAP-280 sandbox as the reference, PAP-106 bundle semantics, PAP-130 ADR template.\n\n**Definition of done**\n\n* Conformance table for both adapters attached; one golden task shadow-run recorded; ADR merged with a clear recommendation and trigger conditions; changelog.\n\n**Test plan**\n\n* Unit: none beyond the conformance suite.\n* E2E: flag flip on staging for one task.\n\n**Demo**\n\nRun `paperos module conformance agents --impl sandbox,openhands` and read the two columns; open the ADR's recommendation. Under one minute.\n\n**Edge cases**\n\n* Runtime cannot mount a worktree: `spawn` reported `unsupported`; the ADR notes it as disqualifying for builders, possibly acceptable for reviewers.\n* Time box exceeded: stop, record partial results, recommend `do not pursue now`.\n* Runtime needs credentials the broker cannot inject: refused; documented.\n\n**Dependencies**\n\nHard: PAP-469, PAP-280. Soft: PAP-472, PAP-106, PAP-299, PAP-130.\n\n**Agent**\n\nBuilder: Scout (Library Evaluator) with Atlas. Reviewer: Sentinel (Security Auditor).\n\n**Size**\n\nS: half a session."
  }
 ],
 "amendments": [
  {
   "identifier": "PAP-103",
   "section": "Spec",
   "append": "* Effort and model precedence (round 4, aligns with CLAUDE.md): `effort` enum is `low | medium | high | max`; `xhigh` in existing sheets maps to `high` (documented alias accepted by the parser, normalised on build). A character's `model` and `effort` are defaults only: the issue's `Model` and `Effort` labels override them for the session (`resolveModel()` in the pm-linear round-4 routing issue), and `fallbackModel` participates in the overload chain, never in refusal handling."
  },
  {
   "identifier": "PAP-284",
   "section": "Spec",
   "append": "* Defaults (round 4): `roster.yaml` lead defaults follow the plan's per-issue rule rather than a flat Fable 5.1: leads default to `claude-opus-5` / `high` as the fallback when an issue carries no Model label, Sentinel reviewer subs to `claude-opus-5` / `high` in `plan` mode (Sonnet 5 / high when the builder was Sonnet, decided per review by `resolveModel`), read-mostly subs to `claude-sonnet-5` / `medium`; Fable 5.1 is reserved for the eight keystone specs and the four release-candidate reviews as labelled in Linear."
  },
  {
   "identifier": "PAP-106",
   "section": "Scope",
   "append": "* Round 4: this issue is split into two children, `Least privilege: per-character runtime bundles and the enforce-scope hook` (`r4/agents/character-bundles-and-enforce-scope-hook`, M) and `Least privilege: nightly probe suite and the generated privilege matrix` (`r4/agents/least-privilege-probe-suite-and-matrix`, S); this issue becomes their umbrella and keeps the integration test (37-row table). `secretsFor()` is retired in favour of PAP-300 broker placeholders; the bundle's `envNames` list names placeholders only."
  },
  {
   "identifier": "PAP-298",
   "section": "Scope",
   "append": "* Round 4: split into two children, `Deny list: policy file, matcher and hook extension` (`r4/agents/deny-list-policy-file-and-hook`, M) and `Deny list: request-approval skill, MCP destructive interception and backstop matrix` (`r4/agents/request-approval-mcp-interception-and-backstops`, S); this issue is their umbrella and owns the adversarial-suite integration test and the `pnpm policy:backstops` CI gate."
  },
  {
   "identifier": "PAP-299",
   "section": "Scope",
   "append": "* Round 4: split into two children, `Prompt injection: trust tiers, wrapper, scanner, actor verification and spec freeze` (`r4/agents/trust-tiers-wrapper-scanner-and-actor-verification`, M) and `Prompt injection: injection eval suite and canaries` (`r4/agents/injection-eval-suite-and-canaries`, S); this issue is their umbrella. The `blocks` relation to PAP-81 moves to the eval-suite child (fork-PR reviews wait for the first green run) and the relation to PAP-109 to the tiers child."
  },
  {
   "identifier": "PAP-111",
   "section": "Spec",
   "append": "* Sub-agent limits (round 4): a session may spawn at most six concurrent sub-agents and nest at most two levels; the parent's cap covers them (SDK cost already includes sub-agents) and a sub-agent inherits at most the parent's served model, never a dearer one. Exceeding fan-out or depth is denied by the PAP-106 hook (`Task` tool input inspected) and logged as `limit.fanout`. Kill semantics: `kill(session)` cascades to sub-agents through the same abort controller."
  },
  {
   "identifier": "PAP-282",
   "section": "Spec",
   "append": "* Prompt order and context pack (round 4): `renderPrompt` places, in this order, the system prompt, the character prompt, the memory block (PAP-109), the project context pack (`r4/agents/docs-context-pack-and-cache-friendly-prompts`), the umbrella's Module boundary paragraph, then the issue body and untrusted blocks (PAP-299 wrapping), so the prefix is byte-identical across sessions of one character on one project and the prompt cache hits; a snapshot test asserts the order."
  },
  {
   "identifier": "PAP-108",
   "section": "Spec",
   "append": "* Assignee switching (round 4): `to` resolves through `botUser(character)` from the character identity decision (`r4/agents/character-linear-identity-and-attribution`); when no per-character Linear user exists (path B) the orchestrator switches the `Character/<Name>` label instead of the assignee and the handoff comment is authored as the receiving character via `createAsUser`. Both paths are covered by the four-comment dry run."
  }
 ],
 "crossProjectSuggestions": [
  {
   "targetProject": "libraries",
   "title": "MCP catalog: `scope` classes (`readOnly`, `write`, `destructive`), `lastVerified` and a Claude Code plugin manifest per connector",
   "why": "PAP-106, PAP-298 and the plugin packaging issue read scope flags and install connectors; PAP-210 should define the fields and ship each connector as a plugin entry the `paperos-agents` plugin can depend on."
  },
  {
   "targetProject": "collab",
   "title": "Prompt log browser filter by `promptVersion` and `modelServed` with a per-version cost and score column",
   "why": "Round 4 records the prompt version and served model on every session; PAP-135 should expose them so a regression (PAP-310) links to the exact transcripts."
  },
  {
   "targetProject": "pm-linear",
   "title": "`loop.registerPass()` plug-in point in PAP-281 for the promotion pass, SLA evaluator and rate-limit governor",
   "why": "Three round-4 issues extend the poll cycle; one registration API keeps `loop.ts` untouched and testable."
  },
  {
   "targetProject": "identity",
   "title": "Agent principal per sub-character (`pos_agent_` keys carry `character/sub`) so audit rows attribute to the sub that acted",
   "why": "PAP-60 keys carry `character`; bundles and probes now run per sub, and the audit log (PAP-38) should show `Sentinel/Security Auditor`, not only `Sentinel`."
  },
  {
   "targetProject": "forge",
   "title": "Forge bot display names and avatars matching the Linear character identity decision",
   "why": "Whichever path Justin picks for Linear identity, the forge bots (PAP-48) should carry the same names and avatars so PR authors and Linear commenters match."
  }
 ]
}
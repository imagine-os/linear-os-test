# PaperOS plan critique (round 2, after the deepen agents)

Snapshot: `round2/linear-snapshot-2.json`, taken 2026-09-17T06:05Z (same shape as `linear-snapshot.json`, 03:30Z). Compared against `audit.md`, `changes-0..5.json`, `changes-{characters,contracts,golden-path,schedule,security}.json`, the 25 new Linear documents and the 17 project `content` fields (`_project_content_2.json`).

| Fact | 03:30Z | 06:05Z |
|---|---|---|
| Non-archived issues | 214 | 275 (61 new: PAP-219..PAP-279; 12 gap issues, 49 children of 16 parents) |
| `blocks` relations | 284 | 437 (+154, one removed: PAP-132 → PAP-123 undone because it closed a cycle) |
| Cycles / phase inversions | 0 / 0 | 0 / 0 |
| Milestone date inversions | 6 | 8 (different set; see FIX-1) |
| States | Backlog 186, Ready 21, Duplicate 7 | Backlog 242, Ready 26, Duplicate 7, Needs Justin 0 |
| Descriptions rewritten | – | all 206 canonical (median 4.9k → 4.5k chars); every one of 267 specs now has Goal, Scope, Spec, Interface contract, Definition of done, Test plan, Demo, Edge cases, Dependencies, Agent, Size |
| Documents | 1 (Blueprint) | 26: Contracts, Threat Model, Golden Path, Execution Schedule, Roster + 9 character sheets, 12 "Round 2 pending issues" docs |
| Project descriptions | 17 one-liners | unchanged; every project `content` now has a `## Contract` section (2.7-6.4k chars) |
| Issues Linear refused to create | – | ~165 (`USAGE_LIMIT_EXCEEDED`, free-plan cap) — specified in the 12 pending docs and in `agent*/pending-issues.json`, `gaps-pending-0.json`, `pending-issues-contracts.json`, `pending-issues-golden-path.json` |

Integrity checks on the new snapshot: no duplicate titles inside a project (the only exact duplicates are the known PAP-6..12 strays versus PAP-13..19), no duplicate relation pairs, no relation to a missing issue, no orphan child, no child in a different project or milestone than its parent, no child with a different Phase than its parent, no issue without Phase, Type, surface label, milestone or a full description. The agents did not break labels, descriptions or the DAG. What they did break is in FIX-1 to FIX-4.

## 1. Scores before and after (1-5 per axis; Coverage / Precision / Buildability / Testability)

| Project | Before | After | Change and reasoning |
|---|---|---|---|
| app-shell | 5/5/4/4 = 18 | 5/5/4/4 = 18 | PAP-19/20/21/28 split into 12 children; golden-path doc answers PAP-5 in minutes; but runtime flags, onboarding wizard, code signing, custom domains, client errors and all 8 golden-path build issues are still only in documents, and the children carry none of their parents' blockers (FIX-3). Device recordings still unverifiable by a Linux session. |
| data-layer | 5/5/4/5 = 19 | 5/5/4/5 = 19 | PAP-279 filter grammar exists and is Ready; PAP-35/36 split; +31 relations; Contracts document fixes Money, Principal, ids, RLS context. Buildability stays 4: chain 13 → 42 → 32 → 33 is still serial, PAP-279 is Ready while a relation says PAP-13 blocks it (FIX-2), and event bus, email package, encryption and idempotency exist only as pending issues. |
| forge | 4/5/4/5 = 18 | 4/5/4/5 = 18 | PAP-45 and PAP-54 split; non-Linux runners and template upgrade still pending only. PAP-54 children were created although the audit said defer, which spent three of the last issue slots on P2 prio-3 work. |
| identity | 4/5/4/5 = 18 | 5/5/4/5 = 19 | Threat model PAP-219 (Ready), session management PAP-220, privacy PAP-221, developer keys and webhooks PAP-222 created; PAP-57/59/65 split. Coverage now complete. Buildability 4 because PAP-223..229 children carry no external blockers and PAP-65's children were created despite deferral. |
| design-system | 4/5/5/4 = 18 | 5/5/5/5 = 20 | Date pickers PAP-233, state components PAP-234, email and PDF theme PAP-235 created; PAP-67 split; PAP-73 recast as an automated axe and ARIA-snapshot audit with manual AT handed to PAP-156, so every DoD is now runnable on Linux. Only blemish: PAP-235 is on the deferred list yet blocks PAP-180 (FIX-4). |
| quality | 5/4/3/4 = 16 | 5/5/4/5 = 19 | Gate artifact contract PAP-239, test-mode seeding PAP-240, Gate 2 calibration PAP-241, API perf budgets PAP-242; PAP-81/82/85/88 split into 12 children; PAP-88 retyped Build. Buildability still 4: PAP-239 (milestone 09-25) blocks PAP-97 (09-22) (FIX-1), children lack blockers, LFS and 4-shard runners still assumed. |
| pm-linear | 4/4/4/4 = 16 | 4/5/3/4 = 16 | PAP-91/93/95 rewritten to treat the live workspace as correct (the audit's top three weaknesses are fixed) and the Execution Schedule adds day-by-day claims and 21 numbered Needs Justin items. Buildability drops: PAP-96 (P0, blocks 3) and PAP-101 are now "umbrellas" whose children do not exist and whose bodies have no fallback clause (FIX-5); nothing anywhere defines how a Backlog issue becomes Ready when its blockers finish (FIX-8). |
| agents | 5/4/3/4 = 16 | 5/5/3/4 = 17 | Roster document plus nine character sheets make the org precise and linked from the project content. PAP-104 (P0, blocks 9) is an umbrella whose four children do not exist and has no fallback (FIX-5); the runtime sandbox, session observability and all 11 security issues are pending only. |
| spec-builder | 5/5/4/5 = 19 | 5/5/4/5 = 19 | Golden Path document; +4 relations. Spec i18n, spec versioning and the children of PAP-119/120/124 are pending; PAP-114 (Ready) lists pending `spec-builder/spec-versioning` as a hard dependency, which a cold session cannot resolve. |
| collab | 4/4/4/4 = 16 | 4/5/4/4 = 17 | PAP-136 moved to P1 with notification core as work package 1 and PAP-43 as its only hard blocker; PAP-133 milestone fixed; work packages written into PAP-131/132. Relation PAP-131 → PAP-136 still points the wrong way, and runtime docs store, in-app help and push transport are pending only. |
| realtime | 5/5/4/4 = 18 | 5/5/4/4 = 18 | PAP-139 → PAP-140 and permission-engine relations added; PAP-143 work packages. Load test still needs a second generator host; push transport pending. |
| input | 5/5/3/3 = 16 | 5/5/4/4 = 18 | PAP-159 reduced to Web Speech only with Whisper as a reopen criterion; PAP-156 tiered so Tier 1 (ARIA snapshots) runs on Linux today and Tier 2 waits for runners without blocking; PAP-73 hand-off agreed. Runners still exist only as a pending forge issue. |
| tables | 5/5/4/4 = 18 | 5/5/4/4 = 18 | PAP-279 owns the grammar PAP-161/163/166 import; +5 relations; 24 pending children and gaps written. Buildability held back by the schedule agent moving "Grid with sort, filter, group" to 09-28, after "All view types" 09-27, which inverts PAP-163 against PAP-167/168/169/170 (FIX-1). |
| business-core | 4/5/3/4 = 16 | 4/5/4/4 = 17 | Money settled (bigint runtime, string wire, PAP-71 agrees with PAP-175); PAP-176 Ready; NJ-10 (Stripe) and NJ-12 (payroll) are dated in the schedule; +8 relations. Usage metering and recurring dunning pending; PAP-174 and PAP-180 cite pending `recurring-dunning` as a hard dependency. |
| growth | 4/4/2/3 = 13 | 4/5/3/4 = 16 | Every description rewritten with dryRun adapters and mock providers, so DoDs no longer require live sends; PAP-196 deferred in its body; PAP-188 Ready. Schedule defers 190, 191, 193-197 to v0.2, which leaves CRM core buildable, but PAP-190 (deferred) still blocks PAP-192 (scheduled 09-28) (FIX-4); consent centre pending is a hard dependency of PAP-187/191/193. |
| migration | 4/5/3/4 = 16 | 4/5/3/4 = 16 | PAP-198 Ready; +6 relations; 27 children and 2 gaps fully specified but pending. `migration/test-accounts` is a hard dependency of PAP-200/202/203/204/206 and does not exist; without it every importer DoD is unverifiable. |
| libraries | 5/4/5/4 = 18 | 5/5/5/4 = 19 | PAP-213/214/215 rewritten with per-work-package time boxes; PAP-209/210 Ready; project content links the rubric. |
| **Total** | **289 / 340** | **304 / 340** | +15. Precision is now 5 everywhere; the remaining deficit is buildability, and most of it is one cause: the issue cap. |

## 2. What improved

- The audit's fifteen weakest issues are all addressed in Linear text: PAP-91/93/95 match the live workspace; PAP-104, 57, 96, 81, 82, 67, 19, 35, 36, 45, 88, 85 are split or describe their split; PAP-73 and PAP-156 no longer overlap; PAP-159 is TypeScript-only; PAP-190 ships adapters in dryRun; PAP-136 is P1; PAP-65, PAP-196 and PAP-54 say "deferred" in their bodies; PAP-88 and PAP-110 are Type/Build.
- The three plan relations missing in Linear (PAP-25 → 96, PAP-120 → 29, PAP-28 → 29) and the eleven hard spec dependencies are encoded; 154 relations added in total; the six original milestone inversions are gone (four new ones appeared, FIX-1).
- Cross-project interfaces that the audit called unowned now have a single written source: the Contracts document (ids, RLS context, `Principal`, `Money`, `FilterTree`, event envelope, package boundaries, gate artifacts), PAP-279 (filter grammar, Ready, blocks 8), PAP-239 (gate artifacts), PAP-240 (test seeding, so Gate 3 no longer depends on P1 PAP-86), PAP-219 (threat model, Ready).
- Every spec gained an Interface contract (provides / consumes / requires), a Test plan and a Demo script, which is what a reviewer session needs to verify a PR without Justin.
- The schedule document gives each of the 251 schedulable units a start half-day, a capacity curve (8 → 20 → 6 sessions), RC0-RC3 checkpoints, a credit line ($8.3k of $10k) and a stop-loss on 09-27.
- The character sheets and roster give each session a persona, routing rules and shared rules; each project's content links its lead.

## 3. Fix briefs

Ordered as requested: breakage by the deepen agents first (FIX-1..4), then the most valuable remaining weaknesses (FIX-5..12). Each brief is self-contained for one agent editing Linear through the API (`linear:linear-api` skill, proxy, aliases of up to 10 mutations, 300 ms between requests, 60 s back-off on 429/RATELIMITED, ids from `round2/linear-snapshot-2.json`, log every id in your own `changes-*.json`). None requires creating an issue; the cap makes that impossible until FIX-5 lands.

### FIX-1 (breakage) — Eight milestone date inversions, four created by the schedule agent's milestone moves

**Wrong.** A blocker sits in a milestone dated after the milestone of the issue it blocks, so the board and the schedule disagree:

| Blocker (milestone, date) | blocks | Blocked (milestone, date) |
|---|---|---|
| PAP-163 view compiler (tables "Grid with sort, filter, group", 09-28) | → | PAP-167, PAP-168, PAP-169, PAP-170 (tables "All view types", 09-27) — created when `changes-schedule.json` moved Grid from 09-23 to 09-28 |
| PAP-239 gate artifact contract (quality "Gates 1 and 2 on every PR", 09-25) | → | PAP-97 webhooks (pm-linear "Orchestrator claims and ships issues", 09-22) |
| PAP-30 Postgres provision (data-layer "Postgres + Drizzle baseline", 09-22, moved from 09-19) | → | PAP-26 deploy pipeline (app-shell "Template scaffolds and runs on web", 09-20) |
| PAP-235 email/PDF theme (design-system "Themable per tenant with docs", 09-30) | → | PAP-180 invoices (business-core "Ledger and reports", 09-29) |
| PAP-88 release train (quality "Edge-case hunting and release trains", 09-30) | → | PAP-29 drill (app-shell "Multi-monitor and PWA polish", 09-29) — left over from the audit, still unfixed |

**Fixed looks like.** Zero inversions from the check in `_analysis` style (blocker milestone date ≤ blocked milestone date for all 437 relations). Concrete edits, all `issueUpdate`/`projectMilestoneUpdate`, no state changes: (a) tables: set "All view types" targetDate to 09-29 and "View sharing, formulas, dashboards" to 09-30 (keep Grid at 09-28, which the schedule needs); (b) PAP-239 → PAP-97: PAP-239 already sits in the earliest quality milestone and PAP-97 is P0, so no milestone move can fix it; make the edge soft instead — delete the relation (id in the snapshot) and write in both Dependencies sections "PAP-239 soft: PAP-97 ships its own `orchestrator-event.json` schema and adopts `packages/contracts` when it lands"; (c) PAP-30 → PAP-26: PAP-26's staging deploy needs a database, so move PAP-26 to app-shell milestone "Desktop and mobile shells build" (09-24) or set "Template scaffolds and runs on web" to 09-22; prefer the latter and update the schedule doc line; (d) PAP-235 → PAP-180: make soft (PAP-180 already renders PDFs with its own template; PAP-235 unifies later) and delete the relation; (e) PAP-88 → PAP-29: make soft (PAP-29 says the drill runs weekly; the first run does not need the release train) and delete the relation. Record deleted relation ids and update the Dependencies sentence in each affected issue so text and graph agree.

### FIX-2 (breakage) — Ready for Claude contains blocked issues and contradicts its own Dependencies text

**Wrong.** PAP-279 (new, Ready) has an inbound `blocks` relation from PAP-13 (also Ready, not Done), while its Dependencies section says "None hard … Ready now". PAP-161 (Ready) is now blocked by PAP-279 because agent 0 added PAP-279 → PAP-161. PAP-96's claim guard (`updatedAt` and state only) would happily claim both; PAP-93's `BLOCKED_BY_OPEN` is only a warning. The audit's rule was that the Ready set contains only unblocked issues.

**Fixed looks like.** Either the relations or the states change so that every issue in Ready for Claude has zero open inbound `blocks` relations. Recommended: delete relation PAP-13 → PAP-279 (a Zod package with its own `package.json` does not need the monorepo scaffold; say so in PAP-279 Dependencies) and change PAP-279 → PAP-161 to soft: delete it and rewrite PAP-161 Dependencies to "PAP-279 soft: import the draft `FilterTree` from the PAP-279 PR branch; if PAP-279 is not In Review when you start, define `filter` as `unknown` and leave a TODO referencing PAP-279". Verify with the snapshot query that the 26 Ready issues have no open blockers, and add the invariant as an error code `READY_BUT_BLOCKED` to PAP-93's list so the validator enforces it going forward.

### FIX-3 (breakage) — 49 children inherit none of their parents' blockers, and no umbrella rule exists

**Wrong.** The 16 split parents (PAP-19, 20, 21, 28, 35, 36, 45, 54, 57, 59, 65, 67, 81, 82, 85, 88) keep their inbound `blocks` relations, but their 49 children only have sibling-order relations. Example: PAP-236 says "PAP-66 (hard)" in its Dependencies yet has zero inbound relations; PAP-223..226 do not depend on PAP-33/PAP-56; PAP-270..272 do not depend on PAP-30/31/34/35; PAP-246..248 do not depend on PAP-14/78/239/240. `sched/model.py` line 37 assumed "children inherit parent blockers; dependents of parent depend on all children", but Linear encodes neither. Sixteen children have no blockers at all and sit in Backlog. Nothing in PAP-92 (session playbook), PAP-93 (contract) or PAP-96 (orchestrator) says whether a session claims the umbrella L or the children, or how a parent is closed.

**Fixed looks like.** (1) For each parent P with children C1..Cn, for each inbound blocker B of P, create `B blocks Ci` for every child whose own Dependencies text names B or, when the text is silent, for the first child in build order (the one with no sibling blockers). That is about 110 relations, 11 aliased requests. (2) For each outbound relation `P blocks D`, add `Clast blocks D` where Clast is the last child in order (about 40 relations), so D's readiness follows the real work. Keep the parent relations. (3) Write an "Umbrella rule" paragraph into PAP-92 Spec, PAP-96 Spec (claims work package) and PAP-93 Spec, verbatim: "An issue with sub-issues is an umbrella. It is never moved to Ready for Claude and never claimed; the orchestrator skips it and the validator returns error `UMBRELLA_NOT_CLAIMABLE`. Children are claimed like any issue. When all children are Done, the session that finishes the last child runs the umbrella's integration test, attaches the evidence to the umbrella and moves it to In Review." (4) Re-run the milestone and cycle checks after adding relations (none of these edges can create a cycle because children have no outbound edges outside the family, but verify).

### FIX-4 (breakage) — The schedule's deferred set contradicts the graph and is invisible in Linear

**Wrong.** The Execution Schedule defers 28 issues to v0.2 and states "Nothing scheduled depends on them." False on two counts: PAP-235 (deferred) blocks PAP-180 (scheduled 09-24) and PAP-190 (deferred) blocks PAP-192 (scheduled 09-28). Twenty-six of the 28 deferred issues say nothing about deferral in their descriptions (only PAP-196 and PAP-54's children mention it), they carry Phase P1/P2 labels like everything else, and 24 of them are in Backlog with fully satisfiable blockers, so PAP-96's claim order (least slack, then priority) will claim them the moment their blockers finish and spend credits on v0.2 work.

**Fixed looks like.** Create one team label `Deferred` (ungrouped, `issueLabelCreate`, allowed) and apply it to the 28 issues: PAP-23, 137, 149, 157, 158, 182, 185, 190, 191, 193, 194, 195, 196, 197, 203, 204, 206, 207, 218, 221, 222, 230, 231, 232, 235, 276, 277, 278. Set their priority to 4. Prepend one line to each description under Goal: "Deferred to v0.2 by the Execution Schedule (NJ-14 stop-loss 09-27 can reinstate). Not claimable before 10-01." Resolve the two contradictions per FIX-1 (PAP-235 → PAP-180 soft) and by making PAP-190 → PAP-192 soft (PAP-192, the content agent, drafts posts into the approval queue; publishing waits for PAP-190; delete the relation and say so in both Dependencies). Add to PAP-96 Spec: "never claim an issue labelled `Deferred`". Update the schedule document's sentence to name the two softened edges.

### FIX-5 — About 165 fully specified issues cannot be created (free-plan cap), and the ask is invisible to Justin

**Wrong.** Every `issueCreate` since about 04:20Z returned `USAGE_LIMIT_EXCEEDED` at 275 issues. The uncreated work: 3 pm-linear children of PAP-96, 3 of PAP-101, 4 of PAP-104, 3 of PAP-110 evals, 9 spec-builder children, 22 collab/realtime/input, 48 tables/business-core/growth, 29 migration/libraries, 14 app-shell/data-layer/forge gaps, 11 security, 4 contracts, 8 golden path, plus the pm-linear and agents gaps (runtime sandbox, session observability, weekly re-audit, inbound triage). 49 live issues reference them as `[project/key]`; 20 do so in Dependencies and 12 as hard dependencies (PAP-200/202/203/204/206 → `migration/test-accounts`; PAP-187/191/193 → `gap/growth/consent-centre`; PAP-174/180 → `gap/business-core/recurring-dunning`; PAP-114 → `spec-builder/spec-versioning`). A cold builder on PAP-114 (Ready) cannot resolve its hard dependency. The only place the ask appears is row one of the schedule document ("NJ-1"); the Needs Justin state holds zero issues, so the agreed channel is silent.

**Fixed looks like.** (1) Make the ask visible in the agreed channel: add a top section "Needs Justin — NJ-1" to PAP-91 (Ready, P0, the Linear-configuration issue) stating: "Upgrade the Linear workspace plan (Basic is enough) so ~165 specified issues can be created; until then the plan runs on the 275 that exist. Reply `/approve` on this issue." and move PAP-91 to Needs Justin (this brief instructs the state change; PAP-91's own DoD gains the line "workspace plan upgraded or Justin declined"). Post the same as a comment on PAP-5, which Justin watches. (2) Plan B in the same edit, so no credits wait on the decision: for each of the 12 hard dependencies on pending issues, fold the pending spec into the citing issue as a work package (the pending body is in the documents; `migration/test-accounts` becomes a Needs Justin ask inside PAP-198, which is Ready and Research; `consent-centre` becomes work package 0 of PAP-187; `recurring-dunning` becomes work package 4 of PAP-180 and PAP-174 drops it; `spec-versioning` becomes an explicit out-of-scope with a `migrations/` skeleton in PAP-114) and change the sentence in each Dependencies section from a bracketed key to the new work package. (3) When the cap lifts, create in this order and nothing else first: `agents/runtime-sandbox` (P0 safety), PAP-96's three children, PAP-104's four children, `agents/session-observability`, then the rest by phase; use the existing idempotent scripts (`round2/agent2/create_new.py`, `agent3/create_issues.py`, `agent4/create_issues.py`, `agent5/create_issues.py`, `agent6/create_issues.py`, `apply_golden_path.py`, `create_contracts.py`) after FIX-6.

### FIX-6 — The pending-issue documents contain duplicates and one obsolete issue that the create scripts would all create

**Wrong.** The idempotency check in every create script is "same title in same project", and these pairs have different titles: `security/field-encryption` (agent6, Data Layer) = `gap/data-layer/field-encryption` (gaps-pending-0); `security/platform-dr` = `gap/data-layer/platform-dr`; `security/retention-pii` ≈ `gap/data-layer/retention-pii` + `gap/data-layer/tenant-lifecycle` (hard purge is in both); `contracts/domain-events` = `gap/data-layer/event-bus`; `contracts/idempotency-rate-limits` = `gap/data-layer/rate-limit-idempotency`; `gp/app-shell/upgrade` = `gap/forge/template-upgrade` (both build `paperos upgrade`); `collab/notification-core` duplicates work package 1 that PAP-136 now owns after its P1 re-phase; `pm-linear/workspace-reconcile` is obsolete because PAP-91 and PAP-93 were rewritten to treat the live workspace as correct, which was that issue's entire goal. Also `agents/runtime-sandbox` overlaps `security/credential-broker` and `security/agent-deny-list` (all three define the egress allowlist and no-secrets-in-session rule).

**Fixed looks like.** One agent edits the pending documents and JSON files, not Linear issues: for each pair keep the richer spec (prefer the `security/*` and `contracts/*` versions, which cite the Threat Model and Contracts documents), delete the other from its document's Contents list and body and from its `pending-issues*.json`, and add a line "Merged into `<kept key>`" where it was. Delete `pm-linear/workspace-reconcile` from the pm-linear document and remove the four `[pm-linear/workspace-reconcile]` references in PAP-91, PAP-93 (two) and the pm-linear project content, replacing them with "PAP-91". Delete `collab/notification-core` and re-parent `collab/notifications/*` children to PAP-136. Split the sandbox trio into one issue (`agents/runtime-sandbox`: container, worktree, CPU/RAM) that depends on `security/credential-broker` (egress proxy and secret injection) and `security/agent-deny-list` (hook policy), and fix the cross-references. Result: ~157 pending issues, no two with the same deliverable, every bracket key in a live issue resolving to exactly one pending entry.

### FIX-7 — No "read first" path: the Blueprint is stale and no issue links the five new documents

**Wrong.** The Blueprint document (the only doc a round-1 reader knows) was last updated 02:26Z and links none of the Contracts, Threat Model, Golden Path, Execution Schedule, Roster or character-sheet documents. Zero of the 267 issue descriptions link any of those documents (grep for `interface-and-data-contracts`, `security-and-threat-model`, `golden-path`, `agent-roster`, `execution-schedule` returns nothing). PAP-92 (the session playbook, Ready, "the one document every Claude Code session reads first") does not name them either; PAP-13 (first Ready build) does not point at the Contracts doc it must obey for ids and package boundaries. A cold session claiming PAP-55, PAP-35, PAP-175 or PAP-279 will not find the decisions the Contracts doc already made unless it stumbles on the project content.

**Fixed looks like.** (1) `documentUpdate` the Blueprint with a "Documents (read in this order)" section listing the six documents with URLs and one line each, plus the 12 pending-issue documents under "Waiting on the issue cap". (2) Add a "Read first" line under Goal to PAP-92, PAP-13, PAP-91, PAP-103, PAP-114, PAP-219, PAP-279, PAP-96 and PAP-25: "Read before coding: Blueprint → Contracts (`https://linear.app/paperos/document/paperos-interface-and-data-contracts-d40e6a4d227c`) → your project's Contract section → your character sheet (Roster: `https://linear.app/paperos/document/paperos-agent-roster-org-chart-and-character-index-fc7ea7f41ff3`) → Execution Schedule for your start day". (3) In the Interface contract section of the issues that define or consume a shared type, cite the Contracts doc section: PAP-55, PAP-35, PAP-267, PAP-59 (Principal); PAP-71, PAP-164, PAP-175, PAP-180, PAP-187 (Money); PAP-279, PAP-161, PAP-116, PAP-119 (FilterTree); PAP-28, PAP-97, PAP-136, PAP-174, PAP-177 (event envelope); PAP-78, PAP-80, PAP-82, PAP-83, PAP-84, PAP-85, PAP-89, PAP-239 (gate artifacts); PAP-219 and PAP-80/81/106 (Threat Model). About 35 `issueUpdate` calls, 4 aliased requests. (4) Make PAP-92's DoD include "links every document above and the umbrella, promotion and deferred rules".

### FIX-8 — Nothing defines how a Backlog issue becomes Ready for Claude, or the branch-start rule the schedule relies on

**Wrong.** PAP-96 polls Ready for Claude; PAP-93 only bounces bad issues from Ready back to Backlog; PAP-92 describes claiming; PAP-97/98 handle webhooks and metering. No spec says who moves an issue from Backlog to Ready when its blockers are Done, so after the 26 Ready issues drain (the schedule already starts 17 Backlog issues on 09-18) every session start depends on Atlas doing it by hand, which the schedule admits for 09-17..09-20 only. The schedule's "branch-start rule" (a dependent may start when every blocker is In Review with a PR open and works against the PR branch) is what makes the plan fit before 10-01, but it exists only in the schedule document; PAP-93's `BLOCKED_BY_OPEN` is a warning with no definition of "open".

**Fixed looks like.** Add a "Promotion" work package to PAP-96 Spec and its DoD: every poll cycle, for each Backlog issue in team PAP that (a) has no `Deferred` label, (b) has no sub-issues, (c) has every inbound `blocks` issue in Done, Canceled or In Review-with-PR (the branch-start rule, with the PR branch names collected into the session prompt as `BASE_BRANCHES`), and (d) passes `validateIssue` (PAP-93) with no errors — move it to Ready for Claude with a comment "promoted: blockers PAP-x (Done), PAP-y (In Review, branch feat/PAP-y)". Define `BLOCKED_BY_OPEN` in PAP-93 as "an inbound blocker in Backlog, Todo, Ready for Claude, In Progress or Needs Justin" and make it an error, not a warning. Put the same rule in PAP-92 under "How your issue got to Ready" and add a manual fallback for 09-17..09-20: "Atlas runs `pnpm linear:promote --dry-run` from the orchestrator repo (script specified in PAP-96 promotion package) and applies the list by hand". Add a test to PAP-96's Test plan: fixture graph with a Done chain, an In Review chain with PR, a Deferred issue and an umbrella; exactly the right two issues are promoted.

### FIX-9 — Three P0 umbrellas have no children and no fallback, so the first session that claims them has no buildable scope

**Wrong.** PAP-96 (P0, blocks PAP-97/98/99, scheduled 09-19), PAP-104 (P0, blocks 9, scheduled 09-19) and PAP-101 (P1) were rewritten as "umbrella; the work is split into three/four children" with bracketed pending keys, but the children could not be created and, unlike PAP-131/132/143/151/155/163/164/165/179/180/199..215 (which say "work packages; the umbrella owns the integration test"), these three carry no sentence telling a session what to do when the children do not exist. Their Size reads "L (umbrella; children are M, M, S)". A cold session sees a spec whose Scope delegates everything to non-existent issues.

**Fixed looks like.** In PAP-96, PAP-104 and PAP-101 replace the bracketed child list with an inline "Work packages (build in this order in one or several sessions; if a package's child issue exists, claim the child instead)" list that carries each package's one-paragraph scope and DoD from the pending document (pm-linear doc for PAP-96 and PAP-101, agents doc for PAP-104), keep the umbrella integration test, and restore Size to "L (or three M children when created)". Apply the same audit to the other 12 parents whose "work packages" sentence exists: confirm each package has scope and DoD inline, not just a title. Verify by grepping every description for `\[[a-z-]+/` inside the Scope or Children sections and confirming every hit is accompanied by inline scope text.

### FIX-10 — PAP-136 still depends on comments, and its milestone is a week after its P0 consumer

**Wrong.** The audit called the PAP-131 → PAP-136 edge wrong-direction. The rewrite moved PAP-136 to P1 and made PAP-43 the only hard blocker in text, but the relation PAP-131 → PAP-136 remains ("relation kept"), so promotion (FIX-8) and the schedule (PAP-136 starts 09-27, after PAP-131 on 09-25) both wait for comments. Meanwhile PAP-94 (P0, 09-20) and PAP-89/PAP-88 deliver Justin's queue and digests "as a Linear comment until PAP-136". Justin will receive every Needs Justin card and every gate failure only inside Linear for ten of the fifteen build days.

**Fixed looks like.** Delete relation PAP-131 → PAP-136 and add PAP-131 as soft in text ("mention flows in work package 2 read `comment_thread`; ship package 1 and 3 first"). Move PAP-136 to collab milestone "Docs and prompt log stores" (09-23) and re-order its work packages so package 1 (kinds registry, delivery worker, in-app badge, email via the email package or Resend directly until it exists) is first and sized M with its own DoD ("`issue.needs_justin` and `review.gate_failed` kinds delivered to Justin by email within 2 minutes in staging"). Add relations PAP-136 → PAP-88 and PAP-136 → PAP-89 (soft in text, so they are not blockers but are visible), and change the schedule document row for 09-22 to start PAP-136.

### FIX-11 — Needs Justin asks are scattered across 36 issues while PAP-94 caps the queue at five open cards

**Wrong.** Thirty-six descriptions raise Needs Justin items (PAP-254 five times, PAP-176/130/94/88 four each), the schedule numbers 21 of them (NJ-1..NJ-21), and PAP-94 says the queue holds at most five open cards. Credential asks that the audit wanted batched into one item are still separate: Apple Developer and Windows signing (PAP-256), Stripe test account and Google OAuth consent screen (PAP-177/224/200), payroll sandbox (PAP-176), Airtable/Notion/QuickBooks/Xero test workspaces (`migration/test-accounts`, pending), social platform apps (PAP-190), Resend and Twilio (PAP-191), Webflow (PAP-193). Each will land as its own card on different days, and the ones that take calendar time (Apple enrolment, app reviews, payroll agreements) are asked on 09-19 to 09-25 for work scheduled days later.

**Fixed looks like.** Add one "Credential and account asks (NJ-2 batch)" section to PAP-25 (already the infra bootstrap that "batches every credential ask into one Needs Justin item" and is Ready) listing every external account above with: what to create, which issue consumes it, the secret name in sops, and the deadline it must exist by (Apple and social apps first, because of lead time). Replace the individual asks in PAP-256, PAP-177, PAP-224, PAP-200, PAP-176, PAP-190, PAP-191, PAP-193 with "credential from the NJ-2 batch on PAP-25; if absent at claim time, proceed in sandbox/dryRun and mark the DoD line `blocked on NJ-2`". Update the schedule document's NJ column so that NJ-6, NJ-10, NJ-12 and NJ-13 collapse into NJ-2 on 09-17, leaving decisions only (license, domain, roster approval, RC approvals) as separate cards. Result: at most five open Needs Justin cards at any time in the schedule, and no session ever creates a credential ask.

### FIX-12 — The Ready set is research-heavy and thin on day one; four cheap P0 issues could be made claimable

**Wrong.** Of the 26 Ready issues, 9 are Research and 10 are Spec; the only builds are PAP-13 (scaffold), PAP-66 (tokens) and PAP-25 (infra). The schedule wants 8 parallel sessions on 09-17 and 12 on 09-18 including PAP-16, 17, 30, 32, 42, 46, 78, 236, 273 — all Backlog and blocked only by PAP-13 or PAP-25, which will be In Review within hours. Without FIX-8's branch-start promotion in place, day two stalls on hand promotion. Separately, PAP-44 and PAP-46 (Spec, Ready) and PAP-92 (Docs, Ready) have no `Files:` scope lines, so PAP-99's collision check cannot protect them from PAP-13 writing the same `docs/` paths.

**Fixed looks like.** No state changes; make day two claimable through text and the branch-start rule. Add to PAP-13's DoD "open the PR within 2 hours of claim and post the branch name; dependents start against it" and to PAP-16, PAP-17, PAP-42, PAP-78, PAP-236, PAP-273 a Dependencies sentence "may start when PAP-13/PAP-25 is In Review (branch-start rule); base branch from the PR". Add `Files:` lines to PAP-44, PAP-46, PAP-92 (`docs/adr/0001-*.md`, `docs/pm/branching.md`, `docs/pm/session-playbook.md`) so PAP-99 can run. Add to the schedule document a 09-17 evening step "Atlas promotes the 09-18 starts by hand using the FIX-8 dry-run list". This is the smallest change that keeps sessions 5-12 busy on day two.

## 4. Order for the next agents

1. FIX-5 first (a five-minute edit that unblocks ~165 issues and every other brief's "create" step), then FIX-6 so the creates are clean.
2. FIX-2, FIX-1 and FIX-4 (graph and milestone hygiene; each under 30 mutations).
3. FIX-3 and FIX-8 together (relations plus the umbrella and promotion rules; they define how the queue moves once PAP-96 exists).
4. FIX-9 and FIX-7 (make every P0 issue self-sufficient for a cold session and give it a reading order).
5. FIX-10, FIX-11, FIX-12 (delivery to Justin, batched credentials, day-two throughput).

After these, re-snapshot and re-run the checks in this file's section 0 table; the target is zero inversions, zero Ready-but-blocked, every child with at least one external blocker or an explicit "ready now", and no bracketed key in a Dependencies section that does not resolve to a live issue or an inline work package.

---
identifier: "PAP-965"
title: "MCP catalog: `scope` classes (`readOnly`, `write`, `destructive`), `lastVerified` and a Claude Code plugin manifest per connector"
project: "libraries"
projectName: "Library Discovery & Integration"
phase: ""
type: "Build"
priority: 3
surfaces: ["Agent"]
milestone: null
state: "Triage"
parent: null
children: []
blockedBy: []
blocks: []
key: "r4/triage/53"
url: "https://linear.app/paperos/issue/PAP-965/mcp-catalog-scope-classes-readonly-write-destructive-lastverified-and"
source: "Linear snapshot 2026-09-18T14:58Z (plan/linear-snapshot-live.json)"
updatedAt: "2026-09-18T14:30:13.250Z"
model: null
effort: null
estimate: null
dueDate: null
cycle: null
---

# PAP-965: MCP catalog: `scope` classes (`readOnly`, `write`, `destructive`), `lastVerified` and a Claude Code plugin manifest per connector

**Triage (round 4, 2026-09-18)** — filed from the gap analysis, not yet specified. Priority 3 until Atlas promotes it; no estimate, due date or cycle. Type and Surface labels are provisional.

**Goal**

PAP-106, PAP-298 and the plugin packaging issue read scope flags and install connectors; PAP-210 should define the fields and ship each connector as a plugin entry the `paperos-agents` plugin can depend on. Also suggested: Mark every MCP catalog tool with a `scope` class (`readOnly`, `write`, `destructive`) and a `lastVerified` date (PAP-106 and PAP-298 both read `readOnly` and `destructive` flags from the PAP-210 catalog; PAP-210 should own the field definitions and the verification cadence.)

**Source**

Suggested in `plan/round4/gaps/agents.json, quality.json` (crossProjectSuggestions) for project `libraries`. Nearest existing issue at filing time: PAP-788 (0.38).

**Scope**

To be specified when promoted (round-4 triage skeleton).

**Spec**

To be specified when promoted (round-4 triage skeleton).

**Interface contract**

To be specified when promoted (round-4 triage skeleton).

**Test plan**

To be specified when promoted (round-4 triage skeleton).

**Definition of done**

To be specified when promoted (round-4 triage skeleton).

**Edge cases**

To be specified when promoted (round-4 triage skeleton).

**Dependencies**

To be specified when promoted (round-4 triage skeleton).

**Agent**

To be specified when promoted (round-4 triage skeleton).

**Size**

To be specified when promoted (round-4 triage skeleton).

**Demo**

To be specified when promoted (round-4 triage skeleton).

*Round 4 critique fix (2026-09-18):* resolved 1 round-4 file key in this description to Linear identifiers: `r4/business-core/tenant-metered-billing` = PAP-788.

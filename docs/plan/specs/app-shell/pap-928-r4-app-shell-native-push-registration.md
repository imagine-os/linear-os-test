---
identifier: "PAP-928"
title: "`r4/app-shell/native-push-registration` implements `PushProvider` from `r4/realtime/web-push` and reuses the `push.deliver` job"
project: "app-shell"
projectName: "Universal App Shell & Repo Template"
phase: ""
type: "Build"
priority: 3
surfaces: ["Staff"]
milestone: null
state: "Triage"
parent: null
children: []
blockedBy: []
blocks: []
key: "r4/triage/15"
url: "https://linear.app/paperos/issue/PAP-928/r4app-shellnative-push-registration-implements-pushprovider-from"
source: "Linear snapshot 2026-09-18T14:58Z (plan/linear-snapshot-live.json)"
updatedAt: "2026-09-18T14:07:49.588Z"
model: null
effort: null
estimate: null
dueDate: null
cycle: null
---

# PAP-928: `r4/app-shell/native-push-registration` implements `PushProvider` from `r4/realtime/web-push` and reuses the `push.deliver` job

**Triage (round 4, 2026-09-18)** — filed from the gap analysis, not yet specified. Priority 3 until Atlas promotes it; no estimate, due date or cycle. Type and Surface labels are provisional.

**Goal**

Round-4 dedup: realtime dropped its own mobile-push child so APNs, FCM and `push_device` have one owner; the sender job and payload rules stay in realtime.

**Source**

Suggested in `plan/round4/gaps/realtime.json` (crossProjectSuggestions) for project `app-shell`. Nearest existing issue at filing time: PAP-208 (0.38).

**Scope**

To be specified when promoted (round-4 triage skeleton).

**Spec**

To be specified when promoted (round-4 triage skeleton).

**Interface contract**

To be specified when promoted (round-4 triage skeleton).

**Test plan**

To be specified when promoted (round-4 triage skeleton).

**Definition of done**

To be specified when promoted (round-4 triage skeleton).

**Edge cases**

To be specified when promoted (round-4 triage skeleton).

**Dependencies**

To be specified when promoted (round-4 triage skeleton).

**Agent**

To be specified when promoted (round-4 triage skeleton).

**Size**

To be specified when promoted (round-4 triage skeleton).

**Demo**

To be specified when promoted (round-4 triage skeleton).

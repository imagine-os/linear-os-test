# Changelog

One entry per planning round of the PaperOS Core Platform plan. Linear (team PAP) is the system of record; every mutation named here has a log file with the request and the response. Dates are the days the work landed in Linear.

## Build loop: from 2026-09-19 (autopilot go)

From 2026-09-19 20:07 UTC this content lives in imagine-os/linear-os-test under docs/plan/; the `linear-builder`, `empty-11` and `empty12` repository references in the bullets below are historical.

* **Go given**: Justin Massion asked the build loop to build the whole Linear plan on autopilot, "until we run out of credits" (Slack #linear-os, `docs/prompts/build-2026-09-19.md`).
* **Operating-mode decision** (`docs/decisions/0001-build-pilot-operating-mode.md`): git-only, no PRs (Justin's org policy) — builders integrate to `main` themselves after a green check, and a dedicated review pass moves the issue to Done; the plan's PR-gated deny-list items are suspended for this loop, everything else on the deny list (Linear deletes/archives, PAP-1..PAP-12, labels, views, documents) stands.
* **Repo mapping**: `paperos-template` = `imagine-os/empty-11`, `paperos-orchestrator` = `imagine-os/empty12` (empty repos Justin provided, to be renamed later); `paperos-infra` (PAP-25) deferred to Needs Justin (NJ-2, Hetzner account) with infra files under `ops/` in the template meanwhile. `imagine-os/paperos` is a different, existing product and is out of scope.
* **Wave 0 launched**: 16 unblocked Ready-for-Claude issues (PAP-13 as the monorepo-scaffold anchor, plus 15 research/docs/package issues) across Opus 5, Sonnet 5 and Fable 5.1 builders; full list in `docs/build-log/2026-09-19.md`.
* New docs: `docs/prompts/build-2026-09-19.md`, `docs/decisions/` (index + ADR 0001), `docs/build-log/` (index, `2026-09-19.md`, `builder-brief-v1.md`).
* **PAP-13 landed, wave 1 launched (01:40 UTC)**: `paperos-template` `main` created (104 files, `pnpm check` green); five more issues landed on `main`, one (PAP-176) branch-pushed awaiting integrator. Wave-1 push contention (~20 concurrent builders, PAP-176 lost 10 races) prompted builder brief v1.1 (`docs/build-log/builder-brief-v1.1.md`) and an integrator merge queue (`docs/decisions/0002-integrator-merge-queue.md`): branch first, two tries on `main`, then a dedicated integrator lands `feat/*` serially. Wave 1 (13 issues) launched on the PAP-13 layout. Full detail in `docs/build-log/2026-09-19.md`.
* **Wave 0 reviewed, wave 1 landing, wave 2 running (02:15 UTC)**: review pass Done on PAP-13 (S1 fixed), PAP-44/46/56/188/198/162/91/209/127, Fail on PAP-176 (S1, remediation in progress); builder brief v1.2 makes the integrator merge queue the only path to `main` (the permission system blocks direct builder pushes); promotion pass moved 24 issues to Ready for Claude; wave 2 (7 issues) launched. Full detail in `docs/build-log/2026-09-19.md`.
* **Wave 1 reviewed, wave 2 landing (02:40 UTC)**: review batch 3 Done on PAP-161/279/103/754 (PAP-754 S1 script-injection fix via `env:`), PAP-302 and PAP-139 pass awaiting integration; PAP-555, PAP-66, PAP-93, PAP-281 landed; promotion pass 2 moved 11 issues to Ready for Claude (555 still blocked, top blockers PAP-433/434/537/43); union-merge of `package.json` corrupted JSON and stalled five branches, so decision 0003 (`docs/decisions/0003-integrator-owns-state-moves-and-rebase-landing.md`) makes the integrator land non-clean branches by rebase with a JSON-aware merge and own the In Progress → In Review / Done moves on landing; builder brief v1.3 adds the rebase-before-push rule; 13 follow-ups filed in Triage. Full detail in `docs/build-log/2026-09-19.md`.
* **Credits out at 03:19, stopping point (19:18-20:00 UTC)**: the coordinating session died at 03:19 UTC when the organization ran out of Claude Tag credits; a fresh Fable 5.1 session took the thread over at 19:18 on Justin's instruction to bring the loop to a stopping point with no new waves. Autopilot ran 2 h 08 m (47 builder sessions in three waves, review batches 1-5, integrator, two promotion passes); Linear PAP at 19:30: 1037 issues, 22 Done, 10 In Review, 22 In Progress, 10 Ready for Claude, 4 Needs Justin. `empty-11` `main` (`d14f76c2`) has been CI-red since 02:06 (Biome `organizeImports` on the union-merged core barrel, plus a stale dependency map, a missing `ownership.json` entry and a root-runner test failure); the fix is on branch `fix/main-green-2026-09-19` (`280e1453`, local gate green) because the permission checker denied a direct push to `main`. 25 `feat/*` branches are parked with unmerged commits that never ran in CI (full table in the build log). Pages on `empty-11` serves a Jekyll README (branch source); the demo link is the blueprint site on `linear-builder`. Proposals awaiting Justin, not decisions: one repository named `paperos-core` (rename `empty-11`, orchestrator under `tools/orchestrator`, plan under `docs/plan`), Pages Source = GitHub Actions. Decision 0004 (`docs/decisions/0004-stopping-point-and-branch-fixes.md`): loop paused, coordinator hotfixes on `fix/*` branches, parked branches stay open, no Linear moves, Needs Justin defaults suspended. Full detail in `docs/build-log/2026-09-19.md`, 19:18 section.
* **Consolidation into `imagine-os/linear-os-test` (19:57-20:40 UTC)**: Justin named the repository he created ("move everything to that as a mono repo"); decision 0005 (`docs/decisions/0005-one-repository-linear-os-test.md`). Template at the root (`empty-11` `main` plus `fix/main-green-2026-09-19`), orchestrator at `tools/orchestrator` as a workspace package, this plan at `docs/plan`, history from all three repositories kept and the 25 parked branches pushed unchanged (commits `9624ede`..`3692e92`). Local gate green (51/51 turbo tasks, 1091 tests, 93.6% lines). GitHub Pages by Actions: hub https://imagine-os.github.io/linear-os-test/, Blueprint at `/blueprint/`, web app at `/app/`. Push events under the `claude` identity trigger no runs, so CI is dispatched by hand; the dispatched run failed only at Coverage on `filter.perf.test.ts` (fix in progress at the time of writing, see the next entry). Old repositories left for Justin to delete. Estimate versus actual (Opus 5): `docs/build-log/estimate-vs-actual-2026-09-19.md`. Models: Fable 5.1, Opus 5, Sonnet 5 (status probe).
* **Web demo placeholder screen (2026-09-20, 21:10-21:35 UTC)**: Justin reported https://imagine-os.github.io/linear-os-test/app/ "doesnt show anything"; assets resolved, but the `apps/web` placeholder route (router is PAP-16) rendered only a heading and the commit hash. Commit `f523061` on `main` renders a landing screen with what exists today, links to the Blueprint, hub, repository, build log and docs map, the shell controls (Sign in, Switch role, Developer mode, Command palette) declared in an actions registry and marked not wired with a toast on activation, an EN/ES toggle, fluid type 360-3840 px, 44 px targets, visible focus and reduced motion respected. Pages redeployed; live page verified headless at 390 and 1920 px with zero console errors (`docs/evidence/web-placeholder-2026-09-20/`). Detail in `docs/build-log/2026-09-19.md`, 2026-09-20 section. Model: Fable 5.1.

## Round 4: 2026-09-18 (gap analysis, sub-features, new projects, Linear features)

Justin's ask (Slack, 07:40 CT): "Make the linear plan better and better. Fill in the plan further, use more features of Linear if possible. Get more detailed on sub features, identify any features you missed." Prompts and replies: `docs/prompts/round-4-2026-09-18.md`. Model: Claude Fable 5.1 for the planning session and its workers.

Before: 493 issues, 18 projects, 54 milestones, 1,224 `blocks` relations, 29 Ready for Claude, 52 umbrellas, 49 Deferred, no estimates, due dates, cycles, triage or initiatives. After (snapshot 2026-09-18T14:58Z): 991 issues (983 canonical: 901 specified, 82 Triage), 23 projects, 69 milestones, 5 initiatives, 3,275 relations, 30 Ready, 86 umbrellas, 204 Deferred, 2,214 points on 815 leaves, 697 due dates.

* **Linear team features** (`plan/round4/changes/linear-team-features.json`, 55 mutations; `plan/round4/linear-team-features.md`): Fibonacci estimates on (S 2, M 3, L 5; 433 leaves), weekly cycles C1/C2/C3 from 2026-09-18 (Thursday start), Triage state, `Surface` label group made exclusive, new `Chunk` label group with `Chunk 1..5`; estimate, due date and cycle set on the 485 canonical issues (`issue-fields.json`).
* **Initiatives, project fields, templates, views** (`linear-initiatives-templates-views.json`, 137 mutations; `docs/linear-features.md`): 4 initiatives (a fifth in phase 2), lead / priority / health / 3 links / 1 update on every project, 6 issue templates and 1 project template, 12 shared views (Needs Justin, Ready for Claude by project, Umbrellas, Deferred, Blocked, Current cycle, Chunk 1-5, Triage inbox).
* **Gap analysis** (`plan/round4/gaps/*.json`, `plan/round4/digest/*.md`): 18 per-project feature matrices against the brief and best-in-class products, 335 new issue specs, 143 amendments, 100 parent assignments, 93 cross-project suggestions; a cross-cutting capability map (167 capabilities: 105 covered, 17 partial, 45 gaps) proposing five projects and twelve cross-project gaps (`digest/cross-cutting.md`).
* **Issue creation, phase 1** (`create-issues.json`; `merge-report.md`): 335 specified issues PAP-498..PAP-832 with labels, estimates, due dates, milestones, 947 new `blocks` edges (14 milestone inversions dropped with soft notes), 143 amendments applied, 53 leaves became umbrellas.
* **Phase 2** (`phase2.json`; `verify.md`): 5 projects created (assistant, workflows, engagement, commerce, platform-ops; 3 milestones, 3 links, 1 update, 1 initiative each), fifth initiative "Operate, measure and comply", 81 issues (75 in the new projects, 56 deferred; 6 cross-project engines PAP-908..PAP-913), 479 relations (9 dropped with soft notes), 21 round-3 edge violations softened (relations deleted, notes on both ends), 362 issues that a cycle side effect had moved to Todo reverted to Backlog with no cycle (rule: cycles hold in-flight work only), 82 Triage skeletons PAP-914..PAP-995 from the unfiled suggestions (`unfiled-suggestions.md`).
* **Schedule and chunk plan** (`plan/round4/sched/`, `plan/round4/chunks-v2.json`, `docs/build-chunks.md`, `docs/cost-and-duration-estimate.md` section 9; `chunks-relabel.json`): the model re-run over 601 scheduled leaves and 195 deferred; mix B (builders on their Model labels, Fable 5.1 on specs, reviews and RC reviews) 5 chunks, $10,450 list, $209 to Justin, 38.6 h at 16 builders 24/7; mix A (all Fable 5.1) 8 chunks, $18,522, $370; v0.2 chunk $3,418 / $68; Chunk labels relabelled to mix B (135 / 125 / 136 / 143 / 62).
* **Critique** (`plan/round4/critique.md`, `critique-tools/`; `critique-fixes.json`, 505 mutations): scores 304 -> 317 of 340 over the 17 round-2 projects, 414 of 460 over 23; FIX-R4-4 resolved 804 `r4/<project>/<key>` mentions in 443 descriptions to identifiers, FIX-R4-5 created 52 hard-dependency edges named in text (4 kept soft), FIX-R4-6 moved PAP-627 and PAP-636 to their parents' milestones.
* **Final fixes** (`final-fixes.json`, 244 mutations, 0 failures): FIX-R4-1 unwound the 19 single-child umbrellas (children de-parented, `parent blocks child`, Model / Effort labels and estimates restored, split comments amended; 105 -> 86 umbrellas); FIX-R4-3 added 156 last-child edges (every umbrella's dependents now wait for its last child; rule sentence in PAP-92 and PAP-96); FIX-R4-8 promoted PAP-754 to Ready for Claude (C1); FIX-R4-9 set the five new projects' target date to 2026-10-16 and the v0.1 / v0.2 line. All integrity checks 0 afterwards (`critique.md` section 7).
* **Repository**: `tools/linear/round4/snapshot4.py`, `tools/linear/gen_specs.py` (983 specs in 23 folders), `tools/blueprint/build_v5.js` (blueprint v5: initiatives, cycles, estimates, Round 4 section, filters; reads `chunks-v2.json`), `docs/linear-features.md`, `docs/prompts/`, Round 4 section in `docs/blueprint.md`, this file. Linear documents: "Round 4 (2026-09-18): gap analysis, new projects and Linear features", "How PaperOS uses Linear", Round 4 section appended to the Blueprint document (URLs in `docs/README.md`).
* Open after the round: FIX-R4-2 (504 scheduled session-days against about 208 of capacity), FIX-R4-7 (82 Triage skeletons to accept, fold or decline), FIX-R4-10 (five hand-rolled approval steps to amend onto `ApprovalPort`).

## Round 3: 2026-09-17 (module system and chunk plan)

* New project **Module System & Swap Tooling**: 14 kernel issues PAP-433..PAP-446 (manifest schema, registry and DI, flag-driven swaps with shadow-run, event schema registry, gateway, UI slots, dependency lint, compatibility matrix, conformance runner, swap playbook, migration adapter kit, config port, docs generator, shell-swap drill) and one contract / conformance / wire trio per module, PAP-447..PAP-497 (51 issues across 17 projects); 18 `Module boundary` amendments to live issues (`plan/module-issues.json`).
* Document **PaperOS Module System** (`docs/module-system.md`); Module map added to the Contracts document; discounted terms added to the Execution Schedule. Rule: a module depends only on contract packages, never on another module's implementation.
* Chunk plan (`plan/chunks.json`, `docs/build-chunks.md`, `docs/cost-and-duration-estimate.md`): 326 buildable issues, mix B 4 chunks ($7,909 list, $158 to Justin), mix A 5 chunks ($11,166, $223), about 37 h at 16 builders.
* Blueprint v4 (`tools/blueprint/build_v4.js`): filterable, sortable graph views (objects map, lanes skill tree, radial tree, images, icons, objects 3D, radial 3D), modules and plug points map, chunk plan; GitHub Pages publishing (`.github/workflows/pages.yml`).
* The 153 pending round-2 issues created as PAP-280..PAP-432 after the plan upgrade to Linear Basic (NJ-1): 109 children of existing umbrellas, 36 new umbrellas, 379 relations; `specs/` generated from the live snapshot.

## Round 2: 2026-09-17 (audit, contracts, security, schedule, characters, golden path)

* Every spec read against the brief and the graph; projects scored on coverage, precision, buildability and testability (289 -> 304 of 340); seven agents deepened the weakest parts: 49 children of 16 split parents, 12 gap issues, 11 spec sections per issue (Interface contract, Test plan, Demo added), 534 relations (from 284), 25 Ready for Claude, 28 Deferred (`plan/round2/`, `plan/round2/changes/`).
* Documents: Interface & Data Contracts, Security & Threat Model, Execution Schedule (day-by-day starts, RC0-RC3, NJ-1..NJ-21, burn model), Agent Roster and nine character sheets, New App in Ten Minutes (golden path), eleven "Round 2 pending issues" documents for the 156 specs held back by the free-plan issue cap (`docs/`, `docs/pending/`).
* Fixes FIX-1..FIX-8 from the second critique: milestone inversions 8 -> 0, ready-but-blocked 2 -> 0, +103 relations so children inherit their parent's blockers and the umbrella rule in PAP-92/93/96, the `Deferred` label, NJ-1 on PAP-91, pending-document dedupe, the read-first path, the promotion rule with the branch-start clause.
* Blueprint v2 and v3 pages (`tools/blueprint/build_blueprint.js`, `build_v3.js`).

## Round 1: 2026-09-16 (the plan in Linear)

* `plan/plan.json`: vision, 15 decisions, 3 phases, credit budget, 9 agent characters, labels, states; 17 projects with 51 milestones and 206 issues built into Linear team PAP by `tools/linear/build_plan.py` (`plan/linear-ids.json`), 284 `blocks` relations, 21 Ready for Claude; the PaperOS Spec issue template and six views.
* Round-1 self-critique (`plan/critique-round1.md`) and build log (`plan/linear-summary-round1.md`); canonical spec JSON in `plan/specs/`.

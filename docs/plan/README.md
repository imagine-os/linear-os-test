# PaperOS Core Platform plan

PaperOS Core Platform is the reusable foundation every future PaperOS app is generated from: one spec-driven TypeScript monorepo template that ships to web, desktop and mobile with a shared data layer, design system, multiplayer, table/views engine, identity for every audience, business modules and a documented ten-minute path from a one-paragraph idea to a deployed app. It is built by parallel Claude Code sessions that pick issues from a Linear queue (team **PAP**) under nine agent characters, with Justin Massion (imagine-os) as the only human in the loop. This repository is the plan: every issue spec, the platform documents, the schedule, the pending backlog, the blueprint page and the scripts that put all of it into Linear.

* Linear workspace: https://linear.app/paperos (team PAP)
* Blueprint page (v5: initiatives, cycles, estimates and the Round 4 section on top of the v4 filterable graph views, module map and chunk plan): https://claude.ai/artifact/RhSgtKN7k64GnVmKFrxf8h (mirrored in `site/`, Pages copy with the WebGL views at https://imagine-os.github.io/linear-builder/)
* Session brief for a Claude Code session picking up a PAP issue: [`CLAUDE.md`](CLAUDE.md)

## Read first

In this order, before touching an issue:

1. [Blueprint](docs/blueprint.md): vision, decisions, phases, budget, project index.
2. [Interface & Data Contracts](docs/interface-and-data-contracts.md): the shapes every project codes against (its Module map section lists every contract package).
3. [Module System](docs/module-system.md): every project is a module, modules depend only on versioned contract packages, the kernel (registry, DI, event bus, gateway, UI slots, conformance runner, swap CLI) is the only place a dependency lives, and the swap playbook that lets the shell or any other module be rewritten and plugged back in.
4. Your project's `Contract` section: the project content in Linear (summaries in `plan/plan.json` under `projects[].description`).
5. [Agent Roster](docs/agent-roster.md) and your [character sheet](docs/characters/).
6. [Security & Threat Model](docs/security-and-threat-model.md).
7. [Execution Schedule](docs/execution-schedule.md): start days, milestone dates, Needs Justin table, discounted terms and the $2,500 chunks.
8. [Build in $2,500 chunks](docs/build-chunks.md) and [Cost and duration estimate](docs/cost-and-duration-estimate.md): what each chunk delivers, mix A / mix B, wall-clock at 16 builders running 24/7 (round-4 plan `plan/round4/chunks-v2.json`).
10. [How PaperOS uses Linear](docs/linear-features.md): every Linear feature the plan uses (estimates, due dates, cycles, triage, initiatives, templates, views) and the substitute for the ones the Basic plan lacks.
9. [New App in Ten Minutes: the golden path](docs/new-app-in-ten-minutes.md).

## Repository map

| Path | What it holds |
|---|---|
| `CLAUDE.md` | Brief for a Claude Code session working a PAP issue: read order, pipeline states, rules. |
| `plan/plan.json` | The master plan: vision, 15 decisions, 3 phases, budget, 9 agents, labels, states, 17 projects with 206 round-1 issues (the 18th project, Module System & Swap Tooling, is described in `plan/module-issues.json`). |
| `plan/module-issues.json` | Round 3: the Module System & Swap Tooling project and the 65 module-system issues (14 kernel issues PAP-433..PAP-446, one contract / conformance / wire trio per module PAP-447..PAP-497) with keys, labels, dependencies and the 18 `Module boundary` amendments to live issues. |
| `plan/chunks.json`, `plan/model-effort.json`, `plan/model-effort-B.json`, `plan/estimate-results.json` | The round-3 $2,500 chunk plan and the per-issue Model / Effort assignments behind the labels; superseded by `plan/round4/chunks-v2.json` (mix B: 5 chunks; mix A: 8). |
| `plan/linear-ids.json` | Linear ids and URLs for the team, states, labels, projects, milestones and every round-1 issue. |
| `plan/specs/` | Canonical round-1 spec JSON (`bucket-*.json`, `gaps.json`): the source text of `specs/`. |
| `plan/critique-round1.md`, `plan/linear-summary-round1.md` | Round-1 self-critique and Linear build log. |
| `plan/round2/` | Round-2 audit, critique, gap analysis, per-project digests, schedule model outputs, and `changes/` (one log per agent and per fix FIX-1..FIX-8, with every Linear mutation). |
| `plan/round4/` | Round 4 (2026-09-18): `gaps/` (per-project feature matrices and issue specs as JSON), `digest/` (18 project digests and the cross-cutting capability map), `merge-report.md`, `verify.md`, `critique.md` (scores, integrity checks, fix briefs FIX-R4-1..10, after-fix results), `critique-tools/` (snapshot, checks, fixes), `sched/` (schedule model and per-issue schedules), `chunks-v2.json` (the round-4 chunk plan), `issue-fields.json`, `r4-ids.json`, `linear-team-features.md`, `snapshot-summary.md`, `unfiled-suggestions.md`, and `changes/` (seven logs, every Linear mutation of the round). |
| `plan/linear-snapshot-live.json` | The latest full snapshot of team PAP (every non-archived issue with description, state, labels with groups, estimate, due date, cycle, project, milestone, parent, children, blockedBy, blocks, attachments, URL, updatedAt; plus team settings, cycles, projects with lead, links and updates, initiatives, templates, views, documents), written by `tools/linear/round4/snapshot4.py`; `specs/` is generated from it. |
| `specs/` | One markdown file per canonical PAP issue (983: PAP-13 upward minus duplicates, including the 82 Triage skeletons), grouped by project key (23 folders), with frontmatter (identifier, project, phase, type, priority, state, relations, key, URL, updatedAt, model, effort, estimate, dueDate, cycle) and the live Linear description as body. Index in `specs/README.md`; regenerated by `tools/linear/gen_specs.py`. |
| `docs/` | The platform documents as published to Linear: Blueprint (with the Round 2 and Round 4 sections), Contracts (with Module map), Module System, Security, Schedule (with discounted terms and chunks), Roster, nine character sheets, Golden Path, plus `build-chunks.md` and `cost-and-duration-estimate.md`. Index with Linear URLs in `docs/README.md`. |
| `docs/linear-features.md` | How PaperOS uses Linear: the feature table (used since which round, how; substitutes for the Basic-plan gaps), initiatives, views, templates and what Linear refused. Published as the Linear document "How PaperOS uses Linear". |
| `docs/prompts/` | The prompts and replies of each planning round as they happened in Slack (`round-4-2026-09-18.md`). |
| `CHANGELOG.md` | One entry per planning round (1-4) with every category of change, its counts and the change-log file names. |
| `docs/pending/` | Historical spec text of the 156 round-2 issues held back by the Free plan cap; all 153 were created as PAP-280..PAP-432 on 2026-09-17 (3 folded into live issues). Key-to-identifier table in `docs/pending/README.md`. |
| `site/` | The blueprint page v5 (`index.html`, `data.json`; initiatives, cycles, estimates and the Round 4 section on top of the objects map, lanes skill tree, radial tree, images, icons, objects 3D and radial 3D views with filters and sorting, module map and chunk plan), served by GitHub Pages from the `gh-pages` branch. Earlier versions under `site/previous/`. |
| `tools/linear/` | The Python scripts that built and fixed the plan in Linear (idempotent, `LINEAR_API_KEY` via the proxy); `tools/linear/round4/snapshot4.py` is the current snapshot writer and `tools/linear/gen_specs.py` regenerates `specs/`. |
| `tools/blueprint/build_v5.js` | Builds `site/index.html`, `site/data.json` and (with `PAPEROS_ARTIFACT_OUT`) the self-contained artifact body from the snapshot, `plan/round4/chunks-v2.json` and `docs/linear-features.md`; `build_v4.js`, `build_v3.js` and `build_blueprint.js` are the earlier builders. |
| `.github/workflows/pages.yml` | Publishes `site/` to `gh-pages` on every push to `main`. |

## Status (2026-09-18, snapshot 14:58Z)

* **991 issues** in Linear team PAP (983 canonical from PAP-13 upward: 901 specified, 82 Triage skeletons; plus PAP-5, Justin's original brief, and 7 duplicates) across **23 projects**, 69 milestones, 5 initiatives and 3 phases, deadline 2026-10-01. Workspace on Linear Basic. Snapshot `plan/linear-snapshot-live.json` taken 2026-09-18T14:58Z by `tools/linear/round4/snapshot4.py`; **3,275 `blocks` relations**, no cycles, no milestone or due-date inversions, no deferred -> scheduled edge.
* **30 Ready for Claude** with zero open blockers, all in cycle C1; 872 Backlog behind dependency edges; 82 Triage. 86 umbrellas, 815 specified leaves (2,214 Fibonacci points, S 2 / M 3 / L 5), 697 due dates, 204 `Deferred` (v0.2, priority 4, no due date). Chunk labels (mix B): Chunk 1..5 on the 601 scheduled leaves.
* **Round 4 (2026-09-18, gap analysis, new projects, Linear features):** 18 per-project digests and a cross-cutting capability map (`plan/round4/digest/`) benchmarked every project against the brief and best-in-class products; 416 specified issues and 82 Triage skeletons created (PAP-498..PAP-995), 143 amendments to live issues, 100 children attached to existing issues; five new projects [Tenant AI Assistant & Business Agents](specs/assistant/), [Workflows, Approvals, Forms, Documents & E-Signature](specs/workflows/), [Scheduling, Messaging & Customer Engagement](specs/engagement/), [Commerce, Operations & Vertical Packs](specs/commerce/), [Platform Operations, Analytics & Compliance](specs/platform-ops/) (75 issues, 19 scheduled for 10-01, 56 deferred); estimates, due dates, weekly cycles, Triage, initiatives, project leads/priorities/links/updates, templates and views turned on (`docs/linear-features.md`); the schedule and chunk model re-run over the expanded graph (`docs/build-chunks.md`, `plan/round4/chunks-v2.json`); a critique (`plan/round4/critique.md`, project scores 304 -> 317 of 340 over the 17 round-2 projects, 414 of 460 over 23) with fixes FIX-R4-1, 3, 4, 5, 6, 8, 9 applied and FIX-R4-2, 7, 10 open. Change logs: `plan/round4/changes/` (seven files); prompts: `docs/prompts/round-4-2026-09-18.md`; summary: `CHANGELOG.md`.
* **Chunk plan (round 4)**: at Justin's discounted terms ($50 per $2,500 of list-price Claude spend), 16 builders running 24/7, the 601 scheduled leaves take **5 chunks with mix B** (builders on their `Model` labels, Fable 5.1 on Spec / Research, reviews and the 4 RC reviews: $10,450 list, **$209 to Justin**, 38.6 h) or **8 chunks with mix A** (all Fable 5.1: $18,522 list, $370); the 195 deferred leaves are an optional v0.2 chunk ($3,418 list, $68). Mix B is the recommendation and the Chunk labels follow it.
* **Blueprint v5** (same artifact URL, `site/index.html`, `tools/blueprint/build_v5.js`): initiatives, cycles, estimates (points per project, burn-up), Triage and Needs Justin tiles, a Round 4 section (before/after, 23 projects, twelve gaps, the Linear features table) and cycle / points / initiative / round-4 filters on top of the v4 graph views, module map and chunk plan.
* Round 3 (2026-09-17): project [Module System & Swap Tooling](https://linear.app/paperos/project/module-system-and-swap-tooling-8f81243eebfc), kernel issues PAP-433..PAP-446, one contract / conformance / wire trio per module PAP-447..PAP-497, document [PaperOS Module System](https://linear.app/paperos/document/paperos-module-system-8007373cc6bb); rule: **a module depends only on contract packages, never on another module's implementation.** Round 2 (audit, contracts, security, schedule, characters, golden path, FIX-1..FIX-8) and the creation of the 153 pending issues PAP-280..PAP-432 are complete; logs in `plan/round2/changes/`.

Linear is the system of record. Where this repository and Linear disagree, Linear wins and the file here is a snapshot to be refreshed.
